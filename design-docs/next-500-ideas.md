# Next 500 ideas (third batch)

A third batch, generated by a 28-domain multi-agent fan-out (28 distinct domains, >=30 ideas each), deduped in code against all ~200 prior ideas in [top-100-ideas.md](top-100-ideas.md) and [next-100-ideas.md](next-100-ideas.md), curated in 8 parallel shards, and ranked by value vs effort. Every mapping is meant to name a real code surface.

> Honesty note: after 200 prior ideas, returns genuinely diminish. The strongest ~150 here are worth real attention; the long tail is finer-grained, more speculative, or domain-completionist. Value scores (1-5) are the honest filter - sort by them. Some residual near-duplicates across the 28 domains may survive the title-level dedup.

Effort: S/M/L. Value: 1-5. Total ranked: 500.

---

## Implement-now shortlist

| # | Title | Effort | Why | Sketch |
|---|-------|--------|-----|--------|
| 1 | Contrast-aware label ink per segment | S | makeSimpleLabel hardcodes black ink over identityColor hsl fills, so darker hues render near-illegible text. | In ThreeWheelRenderer.makeSimpleLabel use the passed _color to compute relative luminance and pick #000 or near-white for ctx.fillStyle. |
| 2 | Outline halo behind label glyphs | S | A thin contrasting stroke behind each name decouples legibility from the segment fill and the bloom wash. | In makeSimpleLabel, ctx.strokeText with a small lineWidth before ctx.fillText. |
| 3 | DPR-scaled label canvas for crispness | S | The fixed 256x64 label canvas is blurry on retina where the renderer allows pixelRatio up to 2. | Scale the canvas width/height and font px by min(devicePixelRatio,2) in makeSimpleLabel. |
| 4 | Set window.__spinStarted so the verify probe stops guessing | S | bench/verify.mjs gates on window.__spinStarted which playSpin never sets, so the harness only guesses a spin started. | Set window.__spinStarted=true at playSpin start and false in settle() in ThreeWheelRenderer. |
| 5 | Wire the dead __wheelDebug renderer hook | S | bench/inspect.mjs reads window.__wheelDebug but the renderer never sets it, so snap() always returns null. | Assign window.__wheelDebug = { scene, wheelGroup, renderer } behind an import.meta.env.DEV guard in the ThreeWheelRenderer constructor. |
| 6 | Wire the dead --theme-accent var into chrome | S | ui.updateThemeColor writes --theme-accent on documentElement but no CSS rule reads it, so the color picker does nothing. | In style.css bind --accent to fall back through var(--theme-accent, <current default>). |
| 7 | data-theme attribute instead of className swap | S | toggleTheme sets documentElement.className, clobbering reduced-motion and other root classes and blocking skin coexistence. | Switch ui.toggleTheme to set documentElement.dataset.theme and update the matching CSS selectors. |
| 8 | Resolve picked_id to a name in history | S | HistoryTimeline.formatAction prints a raw UUID stub instead of the eliminated person's name. | In formatAction look payload.picked_id up against roster participants and show the name. |
| 9 | Exact live odds bars in the analytics panel | S | AnalyticsPanel shows only removed counts and a mock biasEffect string instead of real pick odds. | Feed computeAngles(activeParticipants) into AnalyticsPanel and render a percent bar per active name. |
| 10 | Roster odds exposed to screen readers | S | NameList's odds-pct span is aria-hidden and getOdds is never spoken, so SR users get no odds. | Add an sr-only suffix per NameList row naming the person, percent chance, and rank from getOdds. |
| 11 | Resume AudioContext on first user gesture | S | initAudio builds the AudioContext eagerly but browsers keep it suspended until a gesture, so early ticks are silent. | Add a one-shot pointerdown/keydown listener in initAudio that calls audioCtx.resume() and removes itself. |
| 12 | Master gain bus with compressor | S | playTick and playThunk connect straight to destination, so overlapping ticks clip with no headroom control. | Build a shared masterGain into a DynamicsCompressorNode into destination once in initAudio and route both oscillators through it. |
| 13 | Envelope ramps replace the hard tick cutoff | S | playTick flips gain to 0 via a JS setTimeout, producing an audible click at each tick end. | In playTick use gain.setValueAtTime then exponentialRampToValueAtTime over ~35ms on the audioCtx clock. |
| 14 | Mute toggle also silences vibration | S | Haptic ideas add navigator.vibrate calls that ignore the existing wheel-muted flag, splitting one control into two. | Add a private vibrate(pattern) helper in ThreeWheelRenderer gated on this.isMuted and route all vibrate calls through it. |
| 15 | Per-segment vibration pulse on tick cross | S | The spin already detects each peg-cross for audio, so a short pulse makes the drumroll felt as well as heard. | In playSpin's frame loop, beside playTick on a new lastTickSegment, call the muted-gated vibrate(8). |
| 16 | Landing thunk vibration | S | The settle hitstop already fires playThunk, so a matching pulse lands the impact in the palm on the same frame. | In settle() after playThunk(), call the muted-gated vibrate([30,40,18]). |
| 17 | Assertive winner announcement region | S | onWinnerReveal speaks nothing and the only aria-live is polite, so the pick outcome is silent to SR users. | Add an aria-live=assertive span in App.vue fed from onWinnerReveal with 'Picked {name}, {remaining} remaining'. |
| 18 | Native share sheet for links | S | copyLink only writes to the clipboard; on mobile the OS share sheet is the expected affordance. | In App.copyLink branch to navigator.share({url,title}) when available, else fall back to clipboard. |
| 19 | One demo spin from the empty card | M | The empty NameList card already wires SAMPLE_NAMES to add-batch; a one-click demo spin shows newcomers the payoff. | Add a 'Show me a spin' button in NameList's empty-card that emits add-batch(SAMPLE_NAMES) then triggers handleSpin. |
| 20 | Inertia scaled by roster size, not a constant | S | playSpin hardcodes dur 3200 and 4-6 turns, so a 4-name wheel feels as sluggish as a 50-name wheel. | In playSpin scale dur and the full-turn count by sqrt(segmentMeshes.length). |
| 21 | Bloom strength ramps with spin speed | S | bloomPass.strength is a flat 0.6, missing the chance to glow hottest mid-spin and cool on landing. | In playSpin's frame loop drive bloomPass.strength from the eased velocity, peaking near 1.1 and easing back past SLOWMO_START. |
| 22 | Inspector weight control as a real spinbutton | S | The +/- weight buttons step 0.5 but expose no role or keyboard parity, so the control is invisible to AT. | Give InspectorPanel's weight control role=spinbutton with Arrow/PageUp-Down/Home-End handlers firing the existing update-weight emit and aria-valuetext. |
| 23 | Undo a single weight edit inline | S | Weight edits commit immediately with no undo, unlike removals which already push an undo toast. | In App.handleUpdateWeight capture the pre-edit weight and push a ToastStack undo toast whose run() calls updateWeight back. |

---

## Top 50 overall (by value)

| # | Title | Cat | Eff | Val | What it does |
|---|-------|-----|-----|-----|------------|
| 1 | Per-segment vibration pulse on tick cross | Feedback | S | 5 | In playSpin's frame loop, beside the existing onTick/playTick call when segmentIdAtRotation returns a new id, fire navigator.vibrate(8) so each peg-cross is felt as well as heard. |
| 2 | Landing thunk double-tap vibration | Feedback | S | 5 | At the settle() callback that fires playThunk after the 70ms hitstop, also call navigator.vibrate([30,40,18]) so the felt landing lands in the palm on the exact frame the audio thunk plays. |
| 3 | Assertive winner announcement region | Accessibility | S | 5 | App.vue has a single polite aria-live for focusAnnounce and onWinnerReveal speaks nothing, so the pick outcome is silent; add an aria-live=assertive region fed from spin.onWinnerReveal reading 'Picked {name}, {remaining} remaining'. |
| 4 | Snapshot pre-spin state, not post-spin | Backend | S | 5 | memory.rs spin() builds _before_snapshot then discards it and create_snapshot stores parts.clone() after the pick, so undoing a spin restores the already-removed roster. |
| 5 | Which-key pending HUD for leaders | Interaction | S | 5 | useHotkeys today has flat single-key handlers and no chord menu; once a leader key arms a brief window, render a bottom HUD listing each continuation as kbd chips the way Emacs which-key does. |
| 6 | One demo spin from empty card | Feature | M | 5 | Add a Show me a spin button to NameList's empty-card that loads the existing SAMPLE_NAMES via the add-batch emit, runs one handleSpin, then offers to keep or clear the roster. |
| 7 | Causal snapshot lookup by log index | Backend | M | 5 | memory.rs:242 selects snapshots via lexical `s.action_id.as_str() <= before` on random UUIDs, which is not chronological; index snapshots by their position in the session action vector and add an api.rs test that undo-to-action returns the correct prior roster. |
| 8 | Upright-flip labels past six o'clock | Layout | M | 5 | Labels set rotation.z=startAngle+segAngle*0.5 with no hemisphere correction (ThreeWheelRenderer.ts:415), so bottom-half slices render upside-down; add Math.PI plus a radial offset when the mid-angle crosses into the lower hemisphere. |
| 9 | Scrub weight by dragging the odds bar | Interaction | M | 5 | Make NameList's ::before odds bar (--odds-width) draggable; pointer x maps to a target odds percent, inverted through computeAngles to a weight, fed to roster.previewWeight live and updateWeight on release. |
| 10 | Fix snapshot ordering so scrub lands right | Backend | M | 5 | memory get_snapshot compares action_id UUIDs with string <= (the comment admits it) and sqlite ignores before_action_id entirely; resolve 'before' via a real sequence, not UUID lexical order. |
| 11 | Cache and reuse label textures by name | Performance | M | 5 | makeSimpleLabel allocates a fresh 256x64 canvas plus CanvasTexture on every build() per name, and clearGroup never disposes them since labels carry no sharedMaterial flag, so each weight-drag rebuild leaks one texture per name to the GPU. Add a Map keyed by name caching the CanvasTexture and disposing evicted entries. |
| 12 | Spin seed broadcast for shared deceleration | Backend | M | 5 | handlers::spin and SessionEvent::SpinResult send only picked and remaining, so each remote client's playSpin picks its own random offset and rotation count; add a u64 seed and target angle so every client reconstructs the identical cubic-ease path instead of computing a different spin. |
| 13 | Atomic spin action and snapshot write | Backend | M | 5 | In memory.rs and sqlite.rs, spin() commits the participant update, then append_action and create_snapshot run afterward with let _ = so their errors are ignored. |
| 14 | Deterministic spin seed in action payload | Backend | M | 5 | ActionKind::Spin in models.rs records only picked_id; memory and sqlite spin() call rand::rng() with no captured seed. Add a seed plus the active-id set to the Spin payload. |
| 15 | Near-miss hesitation at adjacent boundary | Motion | M | 5 | playSpin in ThreeWheelRenderer solves the target then eases straight in; detect when the resting angle lands within ~0.2 of a slice boundary and add a brief dwell there before creeping to final. |
| 16 | Deep link that pins a history snapshot | Interaction | M | 5 | copyLink in App.vue writes only origin, pathname, and #/<id>; getSnapshot already projects state at any action, so append #/<id>@<actionId> and have the hash router restore that snapshot on load. |
| 17 | Pointer pawl flicks against each peg | Motion | M | 5 | In playSpin, when segmentIdAtRotation crosses a boundary (where playTick already fires) deflect a top pointer mesh a few degrees and spring it back over ~80ms, so the tick sound gets a visible physical cause from the existing pegMeshes. |
| 18 | Monotonic per-session action sequence | Backend | M | 5 | list_actions in sqlite.rs orders by timestamp DESC which collides at ms resolution; add a per-session seq:u64 column incremented under begin_write so snapshot lookup and history scrub get a real total order. |
| 19 | Real presence count over WebSocket | Collaboration | M | 5 | Replace the fake presence span in App.vue:668 (Math.floor(Math.random()*3)+2 peers) with a live count from a server presence_changed event, derived from the Hub receiver_count already tracked per session channel in ws.rs. |
| 20 | Resume action stream after reconnect | Collaboration | L | 5 | handle_ws silently continues on broadcast Lagged with no catch-up, so have the client send its last-seen seq on open and replay missed actions via listActions. |
| 21 | Flick the wheel to spin it | Interaction | L | 5 | ThreeWheelRenderer tracks _isPointerDown but never reads pointer velocity; sample angular velocity around wheelGroup on pointermove and hand the release velocity into playSpin as initial spin energy instead of the fixed dur 3200 cubic. |
| 22 | Flick-to-spin from drag velocity | Interaction | L | 5 | OrbitControls is free when idle; sample pointer angular velocity on release over the wheel and seed playSpin's initial speed and direction from it instead of Math.random turns. |
| 23 | Build a real 2D canvas renderer | Performance | L | 5 | WheelRenderer is the documented abstraction but only ThreeWheelRenderer exists; add a Canvas2D WheelRenderer drawing arcs (mirroring exportWheelSVG angle math) selected via capability or reduced-motion check in WheelCanvas.vue. |
| 24 | Inertial flick-to-spin on the canvas | Interaction | L | 5 | Replace the left/right heuristic in onCanvasClick with pointerdown/move/up velocity capture on the renderer domElement so a fast flick seeds spinDirection and an initial currentRotation velocity into playSpin while slow flicks barely nudge. |
| 25 | Curve each name along its segment arc | Feature | L | 5 | Replace makeSimpleLabel's flat PlaneGeometry(1.6,0.32) with opentype.js glyph outlines (the loaded _otFont, currently only void-touched) laid along the WHEEL_RADIUS arc so each name follows its slice. |
| 26 | Room passcode gate on session create | Backend | L | 5 | CreateSessionRequest in models.rs takes only a title; add an optional passcode the store hashes (argon2) and require an X-Room-Pass header verified in ensure_session_exists before any mutating handler runs. |
| 27 | Drag a wheel segment edge to reweight | Interaction | L | 5 | Add pointer-drag on segment boundaries in ThreeWheelRenderer, mapping the dragged arc delta back to weight via the inverse of computeAngles and calling roster.previewWeight on both neighbors, committing on pointerup. |
| 28 | Capability tokens split host from viewer | Backend | L | 5 | create_session in handlers.rs returns the raw session id used for every action; mint a host token (spin, delete, reset, restore) and a read-only viewer token, and check scope in those handlers. |
| 29 | Eject recoil buzz on elimination | Feedback | S | 4 | Hook the eject TransitionGroup leave in NameList to fire navigator.vibrate([18,30,40]) as the row blurs and flings, timing a rising recoil to the flung-off-the-wheel motion. |
| 30 | Set __spinStarted so verify probe stops guessing | DevEx | S | 4 | bench/verify.mjs grids 0.25-0.75 by 0.7-0.95 clicking the canvas and gates on window.__spinStarted, which playSpin never sets anywhere in src, so set it true at playSpin start and false on settle. |
| 31 | Wire dead theme-accent var into chrome | Feature | S | 4 | ui.ts updateThemeColor writes --theme-accent on documentElement and App renders the color picker, but no CSS rule ever reads --theme-accent, so bind --accent to fall back through --theme-accent. |
| 32 | Contrast-aware label ink per segment | Accessibility | S | 4 | makeSimpleLabel hardcodes ctx.fillStyle '#000' over identityColor's hsl(h,62%,66%) fills; compute the segment color's relative luminance and pick black or near-white ink so darker hues stay legible. |
| 33 | data-theme attribute over class swap | Feature | S | 4 | toggleTheme sets document.documentElement.className, clobbering other root classes and supporting only default and high-contrast; switch to dataset.theme so skins coexist with high-contrast and reduced-motion states. |
| 34 | Exact live odds bars in panel | Feature | S | 4 | AnalyticsPanel only shows removed counts and the mock biasEffect string; feed it computeAngles(activeParticipants) so each active name gets a real percent-to-be-picked bar instead of the fake bias note. |
| 35 | Inertia from roster size not constant | Motion | S | 4 | playSpin hardcodes dur 3200 and full 2pi*(4-6); scale both by sqrt(segmentMeshes.length) so a 50-name wheel coasts longer and a 4-name wheel snaps. |
| 36 | Landing recoil shoves the camera | Feedback | S | 4 | On land we call playThunk inside settle; add a one-frame camera position jolt (a few px, decaying) at the hitstop, reusing markDirty, so the impact kicks the viewport. |
| 37 | Lock CORS to known origins | Backend | S | 4 | main.rs builds CorsLayer with allow_origin(Any); replace it with an ALLOWED_ORIGINS env allowlist so arbitrary sites cannot drive the mutating REST endpoints. |
| 38 | Native share sheet for links | Feature | S | 4 | copyLink in App.vue only calls navigator.clipboard.writeText with the hash URL; branch to navigator.share({url,title}) when available so the Share button opens the OS sheet. |
| 39 | Reduced-motion locked camera | Accessibility | S | 4 | Gate all new camera moves (dolly, shake, FOV) behind the existing prefersReducedMotion check used for bloom and wind-up, keeping the camera at home and skipping tweens for those users. |
| 40 | Grab-lift squash on drag-reorder | Motion | S | 4 | In NameList onDragStart add a class scaling the row 1.06 with a raised shadow and a navigator.vibrate(10), reusing the existing transform transition so the row lifts off the stack when picked up. |
| 41 | Resume AudioContext on first gesture | Backend | S | 4 | initAudio constructs AudioContext in setup but browsers keep it suspended until a user gesture; add a one-shot pointerdown/keydown listener that calls audioCtx.resume() and clears itself. |
| 42 | Resolve picked_id to name in history | DevEx | S | 4 | formatAction in HistoryTimeline.vue:55 prints payload.picked_id.slice(0,8), a raw UUID stub; look the id up in roster.participants and show the eliminated name. |
| 43 | Live odds-change broadcast on weight | Collaboration | S | 4 | InspectorPanel weight edits already emit segment_updated over WS (handlers.rs:296); surface a peer-attributed toast 'Dana set Bob to 3x' and flash the affected NameList odds (getOdds, NameList.vue:58). |
| 44 | Haptic tick on segment cross | Feedback | S | 4 | The playSpin loop fires onTick/playTick on every segment cross via segmentIdAtRotation; add navigator.vibrate(8) gated on the same boundary and a not-muted check. |
| 45 | Outline halo behind label glyphs | Feedback | S | 4 | In makeSimpleLabel, before fillText, stroke the same text with a contrasting ctx.lineWidth so each name has a thin outline, decoupling legibility from the segment's identityColor fill and from bloom wash. |
| 46 | Contrast-checked label text on segments | Accessibility | S | 4 | makeSimpleLabel always paints name text in #000 regardless of segment lightness; compute relative luminance of the seg color and choose black or white ink so labels stay legible on dark hues. |
| 47 | Inspector spinbutton arrow grammar | Accessibility | S | 4 | The InspectorPanel weight control has +/- buttons that step 0.5 but no keyboard parity; give it role spinbutton with ArrowUp/Down=0.5, PageUp/Down=1.0, Home/End=min/max, each firing the existing update-weight emit and updating aria-valuetext. |
| 48 | Announce the active mode for screen readers | Accessibility | S | 4 | When a modal key state engages (leader armed, visual selection, count buffer), write a terse message to the existing polite aria-live region ('chord g', 'count 3', 'selection 4 names') so the keyboard grammar is audible, not just visual in the HUD. |
| 49 | Confirm destructive restore with countdown | Feedback | S | 4 | HistoryTimeline commitRestore() fires immediately; reuse ToastStack's draining-countdown undo window so a restore is itself undoable for a few seconds. |
| 50 | Detent buzz while dragging weight slider | Interaction | S | 4 | In InspectorPanel onWeightInput, fire navigator.vibrate(6) when the previewed value crosses an integer or 0.5 boundary so the range input feels notched like a detented knob. |

---

## All 500 by category

### Motion (34)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Near-miss hesitation at adjacent boundary | M | 5 | playSpin in ThreeWheelRenderer solves the target then eases straight in; detect when the resting angle lands within ~0.2 of a slice boundary and add a brief dwell there before creeping to final. | The tension of almost landing on the wrong name is the emotional peak of any draw wheel and is fully scriptable from the known target. |
| Pointer pawl flicks against each peg | M | 5 | In playSpin, when segmentIdAtRotation crosses a boundary (where playTick already fires) deflect a top pointer mesh a few degrees and spring it back over ~80ms, so the tick sound gets a visible physical cause from the existing pegMeshes. | The tick currently has no visual source; a flicking pawl makes every click a felt boundary crossing. |
| Inertia from roster size not constant | S | 4 | playSpin hardcodes dur 3200 and full 2pi*(4-6); scale both by sqrt(segmentMeshes.length) so a 50-name wheel coasts longer and a 4-name wheel snaps. | A wheel that decelerates identically regardless of size feels weightless; tying coast time to roster size makes momentum legible. |
| Grab-lift squash on drag-reorder | S | 4 | In NameList onDragStart add a class scaling the row 1.06 with a raised shadow and a navigator.vibrate(10), reusing the existing transform transition so the row lifts off the stack when picked up. | Drag reorder currently gives no pickup feedback; a lift-and-buzz makes the row feel grasped. |
| Final-three slow-mo escalation | S | 4 | SLOWMO_START is a constant 0.82; when the active count is small, lower it toward 0.7 and lengthen the sin tail so the last few eliminations get a longer, more agonizing brake. | The drama should peak as stakes rise; scaling slow-mo to remaining players makes the endgame land harder. |
| Bloom strength ramps with spin speed | S | 4 | bloomPass.strength is a constant 0.6; in the frame loop drive it from the eased rotation velocity, peaking mid-spin near 1.1 and easing back by SLOWMO_START so the wheel glows hottest when fastest and cools as it lands. | Coupling bloom to velocity makes the wheel feel like it heats up under speed instead of glowing flatly. |
| Handheld camera shake on thunk | M | 4 | In settle() right after playThunk(), apply a decaying random jitter to camera.position over ~250ms via markDirty frames then snap home, gated behind prefersReducedMotion like other effects. | A short impact shake makes the landing thunk land physically, matching the hitstop freeze already added. |
| Stutter-step ratchet at very low speed | M | 4 | The final approach is smooth sin easing, so below a speed threshold near the end quantize advance into tiny peg-to-peg steps for the last 2-3 crossings. | Real pawl wheels stop in discrete clicks, not a smooth glide, so a few ratchet steps sell the mechanism. |
| Camera dolly-push on the final brake | M | 4 | Controls are disabled during spin and camera.position.z sits at BASE_CAM_Z; in the post-0.82 slow-mo branch ease z from 7.8 toward 6.4 and back, a subtle push-in on the reveal. | A slow push-in during the climax is a classic cinematic beat and the spin already locks the camera so it is safe to drive. |
| Settle micro-oscillation not single overshoot | M | 4 | playSpin does one 0.5deg overshoot then a 70ms setTimeout to target; replace with a damped 2-3 bounce decaying sine so the pointer wobbles into rest like a real ratchet. | A single overshoot snap feels mechanical; a decaying wobble reads as a physical part settling. |
| Dolly-in punch on slow-mo | M | 4 | In playSpin's frame loop, once t crosses SLOWMO_START=0.82, ease camera.position.z from homeCamZ toward ~5.5 alongside the existing sin slow-mo, restoring it on settle(). | The push-in magnifies the final ticks, syncing camera energy to the drumroll the pitch ramp already builds. |
| Friction wobble settle after landing | M | 4 | Extend the settle() overshoot (currently a single dir*0.5deg past rest) into a 2-3 cycle damped sine wobble on wheelGroup.rotation.z, each reversal firing a decaying navigator.vibrate, before locking to target. | A real wheel oscillates a few times against its pawl before stopping, and the current single overshoot under-sells the settle. |
| Probability mass conservation animation | M | 4 | On a spin removal (applySpinResult), animate the eliminated row's odds bar width flowing outward into the surviving rows' ::before fills since computeAngles renormalizes their shares upward. | Dramatizes that a removed name's probability is inherited by everyone left rather than vanishing. |
| Spark burst at the landing peg | M | 4 | In playSpin's settle(), after playThunk emit a one-shot THREE.Points burst at the winning peg's world position (pegMeshes index of winnerId), 30-40 additive sparks with gravity fading over ~600ms via markDirty frames. | The hitstop already sells the landing physically; sparks give it the visual impact a slot-machine stop deserves. |
| Weighted slices bend the tick cadence | M | 4 | segmentIdAtRotation maps rotation onto uniform slices, but build() lays segments by p.angle (weighted); pass the real cumulative angles so ticks fire late on fat slices and rapid-fire on thin ones within one playSpin pass. | Makes a heavy favorite audibly drag the pointer instead of the rhythm lying about slice widths. |
| Two-stage decel with a coast plateau | M | 4 | playSpin uses one cubic ease plus a sin slow-mo after SLOWMO_START 0.82; insert a near-linear constant-velocity plateau in the 0.3-0.6 band so the spin reads as launch, cruise, then brake. | Real roulette wheels hold speed before braking, and a visible cruise phase raises tension before the slow-mo. |
| Smoke wisp trail on removed names | M | 4 | When applySpinResult marks a participant removed, emit a thin grey smoke THREE.Points wisp from that segment's last position before build() rebuilds the wheel. | Eliminations just vanish on rebuild today; a dissipating wisp gives the shame elimination its narrative beat. |
| Trailing glow comet during slow-mo | M | 4 | After SLOWMO_START (0.82) in the frame loop, attach a fading additive comet tail to the segment crossing the pointer so the final candidates leave light streaks as they creep past. | The slow-mo stretch is the dramatic peak; light trails on the finalists heighten suspense exactly when speed is too low to read motion. |
| Velocity-matched motion blur on segments | L | 4 | playSpin computes currentRotation per frame; derive angular velocity and feed it to a radial-blur amount on the output pass, peaking at launch and vanishing into the 0.82 slow-mo. | Sharp text spinning at 6 rev/s looks fake, and speed-coupled blur sells the velocity so the slow-mo reveal pops. |
| Winner rack-focus depth of field | L | 4 | Add a BokehPass after bloomPass in the composer chain (not currently imported) and drive its focus to the winner segment's world Z during settle() so the rim blurs while the landed slice stays sharp. | A shallow focus pull is the strongest cinematic cue that one name is now the subject. |
| Replay run as animated playback | L | 4 | roster.ts replayEvents folds actions instantly; add a timed driver that re-applies each Spin action through ThreeWheelRenderer.playSpin so a finished game replays as a watchable sequence. | Turns the static recap into a shareable highlight reel of the eliminations. |
| Spin-up FOV punch | S | 3 | camera.fov is fixed at 38 with updateProjectionMatrix used only for resize; briefly widen fov a couple degrees on launch and ease back for a speed-rush at release. | A short FOV kick at launch borrows the game-feel trick that makes acceleration feel immediate. |
| Anticipation wind-back scales with power | S | 3 | playSpin uses a fixed wind -dir*0.1 over windDur 180; scale the wind-back angle and duration with intended turns so a big spin visibly loads further before release. | A constant tiny wind-up reads the same for every spin; coupling recoil to power makes the launch feel proportional. |
| Easing curve for resetView | S | 3 | resetView hard-sets camera.position and rotation; tween position to (0,0,homeCamZ) and controls.target to origin over about 350ms with an ease-out, calling markDirty per frame. | The instant snap is jarring after a user has orbited; an eased return feels intentional. |
| Cinematic letterbox during spin | S | 3 | In WheelCanvas animate two CSS bars over the .wheel-bg container in and out when isSpinAnimating toggles, framing the canvas in 2.39:1 only while spinning. | Bars signal the moment matters and tighten attention on the wheel exactly when controls are locked out. |
| Magnetic catch into slice center | S | 3 | randomOffset lands anywhere in the winning slice; in the last 8% of the ease, blend the target from the random point toward the slice center with a softening curve. | A pointer resting near a divider looks ambiguous, and a magnetic pull to center makes the winner read cleanly. |
| Jerk-limited launch ramp | S | 3 | The cubic ease in playSpin starts with zero velocity but unbounded jerk at t=0; prepend a brief smoothstep over the first ~5% so acceleration itself ramps in. | A jerk-limited start feels like a motor spinning up rather than a teleport into motion. |
| Drag-to-reframe with momentum on release | S | 3 | OrbitControls enableDamping just decays in place on release; sample the orbit drag velocity and on pointerup let the camera continue its azimuth sweep a beat before damping. | A momentum release reads more alive than the current immediate damp-to-stop. |
| Drop-settle bounce on reorder commit | S | 3 | In NameList onDrop after emit('reorder'), apply a brief translateY overshoot-settle on the dropped row plus navigator.vibrate(14) so the row falls into place against neighbours. | Gives the drag a felt put-down arc instead of an instant snap. |
| Orbit auto-drift in idle | S | 3 | In startRenderLoop, when not isSpinAnimating and no pointer down, advance controls.autoRotate at a slow speed so the wheel breathes; OrbitControls already supports it and markDirty keeps it rendering. | A gently turning hero shot makes a static idle wheel feel alive without demanding interaction. |
| Anticipation squash adds a recoil buzz | S | 3 | At the start of playSpin where wheelGroup scale is set to 0.97 (the released-spring squash), fire navigator.vibrate(16) so the visible compress-and-release reads as a wound spring letting go. | The squash is a strong existing physicality cue with no felt counterpart; one buzz completes the spring metaphor. |
| Spin energy budget from RNG seed | S | 3 | playSpin draws full turns and randomOffset from independent Math.random calls; derive both from one energy value so a high-energy spin reliably means more turns and a longer coast. | Independent randoms can make a 6-turn spin brake in the same time as a 4-turn one; a shared budget keeps spins coherent. |
| Anticipation tilt before launch | M | 3 | Pair the existing wheelGroup scale squash at playSpin start with a slight camera elevation drop toward maxPolarAngle, recovering as the wheel releases so the camera crouches with the spring. | A camera that loads with the squash and rises on release amplifies the released-spring feel already coded. |
| Odds donut fills proportionally on rebuild | M | 3 | addOddsDonut draws static RingGeometry arcs, so on refreshPreparedSegments animate each arc's theta-length from its previous span to the new one so adding a name visibly squeezes every arc. | Makes the redistribution of probability a felt motion on the wheel, matching the NameList bar transition already present. |

### Feedback (76)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Per-segment vibration pulse on tick cross | S | 5 | In playSpin's frame loop, beside the existing onTick/playTick call when segmentIdAtRotation returns a new id, fire navigator.vibrate(8) so each peg-cross is felt as well as heard. | The wheel already audibly ticks per segment but the felt channel is silent, the single most visceral mobile upgrade. |
| Landing thunk double-tap vibration | S | 5 | At the settle() callback that fires playThunk after the 70ms hitstop, also call navigator.vibrate([30,40,18]) so the felt landing lands in the palm on the exact frame the audio thunk plays. | The hitstop and thunk are the dramatic payoff; a matching heavy buzz turns the freeze into a physical impact. |
| Eject recoil buzz on elimination | S | 4 | Hook the eject TransitionGroup leave in NameList to fire navigator.vibrate([18,30,40]) as the row blurs and flings, timing a rising recoil to the flung-off-the-wheel motion. | The eject animation is the emotional moment of being picked, and a recoil buzz lands the shame in the spinner's hand. |
| Landing recoil shoves the camera | S | 4 | On land we call playThunk inside settle; add a one-frame camera position jolt (a few px, decaying) at the hitstop, reusing markDirty, so the impact kicks the viewport. | The hitstop freeze already implies an impact; a matching camera shake completes the felt collision without touching the wheel. |
| Haptic tick on segment cross | S | 4 | The playSpin loop fires onTick/playTick on every segment cross via segmentIdAtRotation; add navigator.vibrate(8) gated on the same boundary and a not-muted check. | The audio tick ramp is the spin's signature, and matching it with light haptics makes the slow-mo landing land in the hand. |
| Outline halo behind label glyphs | S | 4 | In makeSimpleLabel, before fillText, stroke the same text with a contrasting ctx.lineWidth so each name has a thin outline, decoupling legibility from the segment's identityColor fill and from bloom wash. | A glyph outline survives the UnrealBloomPass glow and varying hues that otherwise eat thin black strokes. |
| Confirm destructive restore with countdown | S | 4 | HistoryTimeline commitRestore() fires immediately; reuse ToastStack's draining-countdown undo window so a restore is itself undoable for a few seconds. | Restore overwrites live state with no grace period, unlike deletes which already get the undo window. |
| Undo a single weight edit inline | S | 4 | On commit in handleUpdateWeight, push a ToastStack undo toast (the pattern handleRemove already uses) whose run() calls updateWeight back to the pre-edit value captured before the change. | Weight edits are easy to overshoot, and an undo toast matches the removal-undo affordance users already know. |
| Envelope ramps replace hard tick cutoff | S | 4 | playTick sets g.gain.value then a setTimeout(40) flips it to 0, which clicks; use gain.setValueAtTime plus exponentialRampToValueAtTime over ~35ms on the ctx clock instead of the JS timer. | Removes the audible zipper click on every segment cross and decouples ticks from setTimeout drift. |
| Vibration cadence tracks slow-mo ramp | S | 4 | Drive each tick's navigator.vibrate length from the same eased t that playTick uses for its 660 to 1200Hz pitch ramp: ~5ms early, lengthening to ~18ms once t passes SLOWMO_START. | Pitch already rises into the landing, so matching the haptic envelope makes deceleration legible through the fingertips. |
| Wire camera-drift detection | S | 4 | In the OrbitControls change handler, compare camera.position.distanceTo(home) to a threshold and call _onDrift; _onDrift, _cameraDrifted, and homeCamZ already exist (currently voided as unused) but never fire. | The entire drift callback chain is dead code; lighting it up unlocks a recenter affordance the UI already expects. |
| Stale-base warning when head advanced | S | 4 | HistoryTimeline preview and restoreTo fetch a snapshot by action id, but the room may have spun since; compare the latest broadcast seq to the snapshot's action and show an inline 'room moved on' badge before applying. | Prevents confidently restoring to a point that silently discards everyone else's recent spins. |
| Tactile metronome on rising tick ramp | S | 4 | Alongside playTick's square-wave ramp, schedule navigator.vibrate on the same per-cross cadence so the felt interval shortens with rotation speed exactly as the audio ticks bunch up near launch. | Speed is encoded in tick density; mirroring it haptically lets a muted or hard-of-hearing user feel how fast the wheel is going. |
| First-spin coachmark on the wheel | M | 4 | Once activeParticipants.length reaches 2 for the first time (tracked via a localStorage flag like wheel-editor), float a one-time Press Space or click to spin coachmark near the dock-spin button, dismissed on first spin. | The Space-to-spin affordance is buried in a tiny kbd-hint, and a timed coachmark lands it exactly when relevant. |
| Preview odds delta as a diff badge | M | 4 | While dragging in InspectorPanel, show a signed badge (+12% / -4%) on each affected NameList row computed from the difference between effectiveForWheel preview and committed odds, clearing on commit or cancel. | Editing one weight changes everyone's odds, and a signed delta per row makes the ripple explicit before committing. |
| Swipe a roster row to remove | M | 4 | Each participant-item in NameList only exposes an x click, so add a pointerdown horizontal-swipe with translateX following the finger that emits the remove event past a threshold, reusing the eject-leave transform. | Swipe-to-delete is the expected touch gesture and the eject animation already exists to land it. |
| Whole-roster odds redistribution arrows | M | 4 | On previewWeight, NameList rows already retint their ::before odds bar via --odds-width, so overlay an up/down arrow and delta-pp chip on every row whose getOdds moved. | Makes the zero-sum nature of weighting visible: one slider move ripples across the whole field, not just the dragged name. |
| Wire canvas hover lift via raycast | M | 4 | onCanvasMouseMove is an empty stub relying on parent setHighlight; add a THREE.Raycaster over segmentMeshes there so canvas hover lifts the segment and emits the id back to light the NameList row. | Hover peek works from the list to the wheel but not the reverse, leaving the canvas inert to the pointer. |
| Snapshot diff strip on hover | M | 4 | HistoryTimeline formatAction shows a one-line label; on hover or scrub compute the participant delta between the hovered snapshot and current roster and render added/removed/reweighted chips beneath the row. | Scrubbing previews the whole wheel but never says what actually changed at that step. |
| Heavier segments buzz longer when crossed | M | 4 | In playSpin pass each crossed segment's weight (already on mesh.userData via the build() emissive logic) into vibrate duration so a 5x name thuds ~25ms while a 1x flicks ~6ms. | Weight is only visual today, so letting mass be felt at each cross makes biased wheels tangibly heavier. |
| Weighted heft preview on segment press | M | 4 | On press-and-hold of a wheel segment (pointerdown timer in the renderer), emit a vibration whose intensity equals that participant's weight from preparedSegments, then release. | Gives a direct tactile readout of bias: press a name and feel how heavy it is without reading the odds number. |
| Live odds in eliminate subtitles | M | 4 | participantCommands subtitle uses 100/list.length; instead read roster.effectiveForWheel weights to compute real pick probability per name so @ rows show true odds under preventRepeat and weight overrides. | The flat percentage lies whenever weights or prevent-repeat are active. |
| Per-context footer key legend | M | 4 | Render a thin always-on legend strip in App.vue whose chips swap by context: idle shows Space/Tab//, a focused row shows arrows/[/]/x, an open palette suppresses it, reading the same keymap descriptor as the shortcuts sheet. | A live context-aware key legend like a terminal status line keeps the grammar in view exactly when it applies. |
| Tick gain follows velocity not just progress | M | 4 | playTick gain is boost?0.032:0.02 keyed only on t>SLOWMO_START; compute per-frame angular delta in the frame loop and scale gain by it so fast early ticks are quieter and slow final ticks louder. | Loudness tracking real speed makes the drumroll deceleration convincing instead of a binary loud/quiet flip. |
| Disabled commands show why, not hidden | M | 4 | CommandPalette's filtered() drops every command where disabled is true; replace the boolean with an optional disabledReason string and render those rows dimmed with the reason as subtitle instead of removing them. | Hiding spin when the roster is empty leaves users hunting; a greyed row with a reason teaches the precondition. |
| Ember rise from the winning segment | M | 4 | In settle(), spawn slow upward THREE.Points embers tinted to identityColor over the winner segment face for ~1.2s, reusing the flame color ramp from useFlame conceptually in 3D. | Connects the app's flame motif to the actual 3D winner instead of leaving fire only in the header title. |
| Tick interval encodes true deceleration | M | 4 | playTick pitch ramps with eased t linearly; instead derive boost and a short stutter from the shrinking real time gap between segmentIdAtRotation crossings, so clicks bunch then space out as the wheel slows. | Ear-perceived deceleration comes from click spacing, not just pitch, so matching it to actual cross intervals makes the slowdown viscerally correct. |
| Emissive donut pulse on each tick | M | 4 | In playSpin's tick branch (id changes from lastTickSegment), ramp the crossed donutMeshes[i] emissiveIntensity (currently 0) up then decay per-frame; the bloom pass already amplifies donut emissive. | Turns the static odds-ring arcs into a live VU meter of the spin without new geometry. |
| Glow ring sweep tracking the pointer | M | 4 | There is no pointer indicator in the 3D scene; add an additive arc mesh at WHEEL_RADIUS whose angle follows segmentIdAtRotation's current crossing in the frame loop, brightening the rim slice under the Math.PI/2 pointer during the slow-mo. | Makes the about-to-win slice legible as the wheel brakes, which the scene currently leaves to chance. |
| Stereo-pan ticks to crossed segment side | M | 4 | playSpin computes the crossed segment index per frame; route playTick through a StereoPannerNode whose pan tracks sin(segment angle) so a peg crossing left of the pointer sounds on the left. | Turns the flat mono drumroll into a spatial sweep that reinforces wheel direction. |
| Highlight matched chars in palette label | M | 4 | fuzzyScore in CommandPalette.vue discards match positions; have it optionally return matched indices and wrap those label chars in a mark element so the scored subsequence is visible. | Match highlighting confirms why a row ranked and teaches which letters matter. |
| Winner stinger swell on slow-mo entry | M | 4 | playSpin crosses SLOWMO_START 0.82 once per spin; trigger a rising filtered-noise or saw swell with a short attack gated on that threshold so tension builds into the landing. | A pre-landing crescendo telegraphs the result moment and lifts the climax. |
| Rumble crescendo on the slow-mo stretch | M | 4 | When playSpin's t passes SLOWMO_START, ramp a continuous low-frequency gamepad rumble from 0.1 to 0.6 magnitude across the remaining window and cut it at settle(). | The slow-mo stretch is the suspense beat, and a swelling rumble under it is the classic game-feel tension build. |
| Highlighted-segment label scale pop | M | 4 | setHighlight lifts a segment by 0.18 in z but the makeSimpleLabel plane stays the same size; also scale the focused segment's label plane up and re-render its canvas at a higher font px so the name enlarges and sharpens. | The current lift moves the label without making the name easier to read, so coupling scale to focus rewards the gesture. |
| Ghost preview of odds before commit | M | 4 | During an InspectorPanel weight drag, render a faint second odds bar in NameList from effectiveForWheel (which already merges previewOverrides) alongside the committed --odds-width, showing current vs proposed odds per row. | previewWeight already drives the wheel; showing the before/after split in the list closes the loop on who you are stealing probability from. |
| Danger styling for eliminate commands | S | 3 | Add an optional tone:'danger' on Command, set it on the participantCommands Eliminate rows, and tint the caret and label red so destructive rows read differently from teal Actions. | Eliminations feel irreversible, so they should not look identical to muting sound. |
| Hitstop silence then impact | S | 3 | playSpin overshoots ~0.5deg and freezes 70ms before settle calls playThunk, so insert a deliberate audio gap during that freeze so the thunk hits into silence. | Silence before the impact sound makes the landing feel weightier, matching the visual hitstop. |
| Tactile tick on Tab focus cycling | S | 3 | In useHotkeys cycleFocusImpl fire navigator.vibrate(4) each time focusedId advances to a new row and a longer 10ms buzz when the index wraps around the roster ends. | Keyboard focus moves silently, so a per-step tick plus wrap bump makes Tab navigation feel detented. |
| Velocity-coupled bloom threshold | S | 3 | bloomPass has fixed strength 0.6, radius 0.5, threshold 0.85, so lower the threshold with angular speed so highlights smear into streaks at peak velocity and tighten as it brakes. | Reusing the existing bloom pass as a speed gauge adds a GPU-cheap sense of energy bleeding off as the wheel slows. |
| Connection-quality pill from RTT | S | 3 | wsConnected is boolean only; combine heartbeat RTT and broadcast Lagged occurrences into a 3-state quality pill (live/laggy/offline) next to the reconnect toast machinery in useSession. | Users learn whether the room is merely slow or actually dropping before the wheel behaves oddly. |
| Diff-aware restore toast | S | 3 | useHistory restoreTo pushes a generic 'Restored to point in history' toast; compute how many participants changed and word it (re-added 3, un-spun 2) using the snapshot delta. | A vague confirmation hides whether the restore did what the user expected at a destructive moment. |
| Weight tooltip with multiplier and odds | S | 3 | Add a tooltip on the InspectorPanel slider thumb during drag showing both the raw 'Nx' and the resulting odds % computed from effectiveForWheel. | A 3x means nothing without knowing it yields, say, 40% of a six-name wheel; pairing them removes guesswork. |
| Spacebar hint reappears when idle | S | 3 | After a session loads with active participants and no spin for about 6s, fade a 'Press Space to spin' hint near the Spin button (it already gates handleSpin on Space) and dismiss it on first Space or hover. | The core verb is keyboard-driven but undiscoverable; a delayed self-dismissing hint teaches it without nagging. |
| Spacebar spin gives a launch kick | S | 3 | In useHotkeys where Space calls deps.handleSpin(), fire navigator.vibrate(22) at the instant the renderer applies its -dir*0.1 wind-up. | The wind-up squash is visual only, and a launch kick ties the felt release to the existing anticipation frame. |
| Anticipation wind-up whoosh | S | 3 | playSpin schedules the launch frame after windDur=180ms with a -dir*0.1 backspin; play a short reverse-swept noise whoosh during that 180ms wind-up window. | Sonifies the existing spring-load squash so the launch has an audible release, not just visual. |
| Settle thunk pitch from impact speed | S | 3 | playThunk is a fixed 140Hz sine on land; scale its base frequency and gain by the residual angular velocity at the start of the 70ms hitstop so a hard landing thunks lower and louder. | A constant landing sound divorces audio from physics, and velocity-scaled impact makes every stop feel earned. |
| Sub-bass thunk layer for impact | S | 3 | playThunk is a single 140Hz sine; add a stacked 55Hz sine an octave-and-a-half below with a fast exponential pitch drop (90 to 50Hz) summed into the same gain. | A felt low-end thump reads as a heavier landing than the thin single tone. |
| Gate label render on font readiness | S | 3 | loadFont calls build([]) once Roboto loads, but makeSimpleLabel relies on the browser font 'bold 28px Roboto' which may not be FontFace-ready; await document.fonts.load before generating label canvases. | Without the readiness gate the first paint can use a fallback font then silently differ from later rebuilds. |
| Minimum-names progress chip | S | 3 | Below NameList's Active heading, show a '1 of 2 needed to spin' chip while activeParticipants.length < 2, counting up as names are added and vanishing once spin is enabled. | A concrete target turns the empty wheel into a small goal the user can complete. |
| Template select previews names | S | 3 | On the create screen, when useCreateSession.templateSelect changes, show the chosen SEED_TEMPLATE's names as a small preview list under the select before Create is pressed. | Picking a template blind gives no sense of who lands on the wheel, and a preview removes the surprise. |
| Per-command keyboard shortcut badges | S | 3 | Add an optional shortcut field to Command (e.g. Space for spin, from useHotkeys) and render it as a kbd chip beside .palette-hint. | Surfacing the direct hotkey nudges users off the palette toward faster muscle memory. |
| Magnetic snap buzz at exact landing angle | S | 3 | During the hitstop overshoot in playSpin, as currentRotation eases from target+overshoot back to target, fire one navigator.vibrate(12) at the instant it crosses target. | Adds a distinct clunk-into-the-detent separate from the heavy thunk, sharpening the sense of a precise lock. |
| Mistype shake on dead chords | S | 3 | When a leader window in useHotkeys expires with no valid continuation or a second key has no binding, flash the which-key HUD red and dismiss it with the chip-shake keyframe already defined in NameList.vue rather than failing silently. | Silent failure is the worst part of modal keys; a 120ms shake tells the user the chord was heard but invalid. |
| Single-name wheel warns before spin | S | 3 | When activeParticipants.length===1 replace the enabled dock-spin with a hint that one name always loses, offering the 'Try sample names' seed; today the wheel spins pointlessly on a single segment. | A one-name spin is a confusing dead end an early guardrail turns into a nudge to add more. |
| Flag duplicate when adding an existing name | S | 3 | NameList.addName goes straight to emit('add'); if the typed name already matches an active participant case-insensitively (the same check parsePastedNames uses), flash an inline 'already on the wheel' note. | First-run users testing the input deserve a clear answer rather than a silent duplicate. |
| Descending tone on elimination | S | 3 | roster remove() sets removed=true and removed_at; hook a downward pitch glide (descending sine 300-120Hz) when a name is eliminated, distinct from the landing thunk. | Gives removal its own falling-out audio identity separate from winning. |
| End-stop buzz at weight clamp | S | 3 | In InspectorPanel where the value is clamped to Math.max(0.1, Math.min(10, val)), when the raw input would exceed the bound fire navigator.vibrate(20) plus a small CSS overshoot-bounce on the slider thumb. | Hitting 0.1 or 10x should feel like a slider slamming a physical end-stop without a glance. |
| Report skipped duplicates on paste | S | 3 | NameList.onPaste already dedupes via parsePastedNames against existing names; in the paste-confirm strip, also report skipped duplicates ('3 new, 2 already on the wheel'). | Silent dedup makes users think names were lost; naming the skips builds trust in the bulk path. |
| Sanitize names rendered in toasts | S | 3 | useSession.ts onMessage builds toast strings from server-pushed names like '${event.picked.name} was picked'; ensure ToastStack renders them as text not HTML and truncates to a safe length. | Names flow from any client through the WS into toast text, so an unescaped render path is an injection risk. |
| Block time-travel during a spin | S | 3 | ui.ts has a spinning flag but HistoryTimeline scrub/restore ignores it, so a user can restore mid-animation and fight ThreeWheelRenderer.playSpin; disable the timeline while spinning. | Restoring while the wheel is mid-spin races the renderer and can leave canvas and roster out of sync. |
| Micro-buzz on the weight stepper | S | 3 | In InspectorPanel adjustWeight(delta), fire navigator.vibrate(5) on each +/- press so the 0.5-step buttons feel like clicking a ratcheted increment wheel. | The minus/plus stepper is the most button-like control in the inspector, so a crisp tactile click makes repeated nudges satisfying. |
| Recenter camera nudge button | S | 3 | resetView() hard-snaps the camera to homeCamZ; when _onDrift reports true, show a control that eases camera.position back over ~400ms instead, giving orbited-away users a signposted, smooth return. | A smooth return beats a jarring instant reset and surfaces a way back that is otherwise hidden. |
| Disabled spin explains itself | S | 3 | The dock spin button is disabled when activeParticipants is empty; add a tooltip and a tiny adjacent caption 'Add 2+ names to spin' so the dead primary action states its precondition. | A greyed-out primary action with no explanation reads as a bug on first run. |
| Paste detector hint on empty input | S | 3 | When NameList.active is empty and the name field is focused, show a subtle hint 'Tip: paste a whole list at once' surfacing the existing onPaste multi-name path that is otherwise undiscoverable. | The bulk-paste feature already works but nobody finds it without a nudge. |
| Spark color matches struck segment | S | 3 | In the tick spark emitter, color each burst with identityColor of the crossed segment from spinSegmentIds so the contact flash reflects whichever name is under the pointer. | Generic white sparks waste the per-segment color already mapped in spinSegmentIds. |
| Natural-language undo description | S | 3 | Replace useHistory's generic 'Undid last action' / 'prior state restored' toast with a description of the reverted Action from the log ('Put Dave back on the wheel'), reusing the ToastStack undo countdown. | Generic undo text leaves users unsure what changed; a specific sentence builds trust. |
| Tick spark at the pointer notch | S | 3 | On each tick in playSpin, emit a few additive sparks at the fixed top pointer (Math.PI/2 at WHEEL_RADIUS) independent of which divider passed, so the contact point itself flickers. | The rising-pitch tick is audio-only; a co-located spark gives the peg-strikes-pointer metaphor a visible contact flash. |
| Bloom threshold dip on each tick | S | 3 | Momentarily lower bloomPass.threshold from 0.85 on each tick so even dim emissive surfaces flare for one frame, then restore, syncing the global glow to the tick cadence. | A single shared bloom parameter pulses the whole scene per tick at near-zero cost, reinforcing the rising-pitch audio visually. |
| Flame flare on spin start | S | 3 | useFlame.flare() already exists and fires only on winner reveal; also call it from the spin-start path so the header fire surges the instant the wheel launches. | flare() is underused, and firing it on launch makes the whole frame react to the spin, not just the canvas. |
| Bottom-anchored toasts above sheet | S | 3 | ToastStack is teleported to body but on mobile the roster sheet occupies the bottom 50vh; offset the toast stack above the sheet so the time-limited undo countdown is never hidden behind it. | A toast buried under the bottom sheet means the user misses their only chance to revert a removal. |
| Hover-lift gives a soft tactile tap | S | 3 | In setHighlight, when a non-null id newly lifts a segment z by 0.18 (line 498), fire navigator.vibrate(4); no buzz on return to rest, so hovering names feels like brushing raised tiles. | The z-lift is already a strong physicality cue; a feather buzz on engage makes the wheel feel like embossed buttons. |
| Stronger thunk haptic on landing | S | 3 | playThunk plays a 140Hz settle tone after the hitstop in playSpin; pair settle() with navigator.vibrate([30,20,40]) so the felt landing matches the freeze and overshoot. | A heavier distinct buzz at the resting angle gives the elimination a decisive physical punctuation. |
| Crash-zoom on prevented repeat | M | 3 | When the landed segment has isPrevented (the red ring built in build()), trigger a fast camera punch-in then pull-out on settle to flag that preventRepeat blocked the result. | A reactive crash-zoom makes the prevent-repeat rejection legible as a deliberate beat rather than a silent re-roll. |
| Gamepad rumble mirrors the spin | M | 3 | Add a gamepad poll in ThreeWheelRenderer's frame loop calling vibrationActuator.playEffect with magnitude scaled by the per-frame rotation delta, plus a strong pulse at the playThunk settle. | Controllers have real dual-rotor rumble phones cannot match, ideal for a group watching on a TV with a pad in hand. |
| Hover-scrub odds donut to compare | M | 3 | addOddsDonut builds per-name arcs with emissiveIntensity 0, so on pointer hover-drag across the donut ramp each crossed arc's emissive and float its percent (reusing getOdds math) at the pointer. | The donut already encodes odds but is currently inert to the pointer. |
| Pointer-segment label live emphasis | M | 3 | segmentIdAtRotation identifies the segment under the pointer each frame during spin, so bump that segment's label to full opacity and size live so the name under the marker is always the most legible. | Reinforces which name the wheel is pointing at frame-by-frame, the moment users care most. |

### Interaction (74)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Which-key pending HUD for leaders | S | 5 | useHotkeys today has flat single-key handlers and no chord menu; once a leader key arms a brief window, render a bottom HUD listing each continuation as kbd chips the way Emacs which-key does. | A leader chord is unusable without a visible menu of what follows, converting a hidden chord into a self-teaching one. |
| Scrub weight by dragging the odds bar | M | 5 | Make NameList's ::before odds bar (--odds-width) draggable; pointer x maps to a target odds percent, inverted through computeAngles to a weight, fed to roster.previewWeight live and updateWeight on release. | Dragging the outcome you want is more legible than a separate 0.1-10 slider. |
| Deep link that pins a history snapshot | M | 5 | copyLink in App.vue writes only origin, pathname, and #/<id>; getSnapshot already projects state at any action, so append #/<id>@<actionId> and have the hash router restore that snapshot on load. | You can share the moment of elimination, not just the ever-moving live session. |
| Flick the wheel to spin it | L | 5 | ThreeWheelRenderer tracks _isPointerDown but never reads pointer velocity; sample angular velocity around wheelGroup on pointermove and hand the release velocity into playSpin as initial spin energy instead of the fixed dur 3200 cubic. | A physical flick is the single most expected gesture for a wheel, but drag currently only orbits the camera. |
| Flick-to-spin from drag velocity | L | 5 | OrbitControls is free when idle; sample pointer angular velocity on release over the wheel and seed playSpin's initial speed and direction from it instead of Math.random turns. | Throwing the wheel with a real flick is the most satisfying gesture a physical wheel offers and is absent today. |
| Inertial flick-to-spin on the canvas | L | 5 | Replace the left/right heuristic in onCanvasClick with pointerdown/move/up velocity capture on the renderer domElement so a fast flick seeds spinDirection and an initial currentRotation velocity into playSpin while slow flicks barely nudge. | A physical wheel is flicked not button-pressed, making this the most natural input for the core action. |
| Drag a wheel segment edge to reweight | L | 5 | Add pointer-drag on segment boundaries in ThreeWheelRenderer, mapping the dragged arc delta back to weight via the inverse of computeAngles and calling roster.previewWeight on both neighbors, committing on pointerup. | Reweighting on the wheel itself is the most direct mental model and removes the trip to the inspector slider. |
| Detent buzz while dragging weight slider | S | 4 | In InspectorPanel onWeightInput, fire navigator.vibrate(6) when the previewed value crosses an integer or 0.5 boundary so the range input feels notched like a detented knob. | The slider currently slides frictionlessly; detent buzzes at meaningful weights give the bias edit a dial-like feel. |
| Spin direction follows drag direction | S | 4 | playSpin already uses spinDirection for the delta and rotation sign; when a flick or grab-drag launches the spin, set spinDirection from the sign of the sampled angular velocity so the wheel spins the way it was thrown. | Honoring throw direction is table stakes for a draggable wheel and the direction term already exists. |
| Sticky scroll keeps active row visible | S | 4 | After updateCaret measures the active row, call scrollIntoView with block:nearest on .palette-list so ArrowDown past the 320px max-height keeps the highlighted row in view. | Long Eliminate lists scroll the caret out of sight with no auto-follow today. |
| Snap weights to clean ratio stops | M | 4 | In InspectorPanel onWeightInput add detents at 0.5/1/2/3/5x: when the dragged value lands within a tolerance of a stop, snap previewWeight to it and emit a short playTick-style cue. | People reason in twice as likely, not 2.3x, and magnetic stops make those intentions reachable. |
| Type a target odds percentage | M | 4 | Make the NameList odds-pct readout an editable field where entering a target percent solves for this participant's weight given the others fixed (inverting computeAngles normalization) and commits via updateWeight. | Stating the outcome you want (make them 30%) is more direct than guessing the multiplier that produces it. |
| Wheel arc-length shows true odds | M | 4 | build sizes segAngle from weight-proportional p.angle but prevent-repeat diverges from that; add a 'show effective odds' mode rebuilding arcs from engine.getEffectiveSegments so a prevented name collapses to zero arc. | The visible wheel currently lies about a prevented name; this makes geometry match the real next-spin distribution. |
| Wire redo into empty stub | M | 4 | useHotkeys has an empty if (e.shiftKey) redo branch; pair it with a redoStack in useHistory that undo() pushes the popped snapshot onto and redo() replays. | A redo affordance is half-built and dangling, so undo currently feels like a trapdoor with no way back. |
| Swipe roster row to remove | M | 4 | NameList rows emit remove(p.id) only via the small times button while the eject TransitionGroup already animates a row sliding off; wire a left-swipe on the row to the same emit so the gesture matches the eject animation. | Swipe-to-delete is expected on mobile lists and reuses the existing fling-off-wheel motion. |
| Press and hold to charge spin | M | 4 | onCanvasClick spins via a crude left/right-half heuristic; replace with pointerdown-to-pointerup hold timing where duration scales playSpin's rotations, with a rim charge ring in --live-accent. | Hold-to-charge gives intent and force to a tap that today is binary and side-dependent. |
| Scrub history via live wheel ghost | M | 4 | previewFromHistory in App.vue:184 only calls roster.previewWeight; also call ThreeWheelRenderer.updateInPlace (currently unwired) so scrubbing rotates and recolors the live wheel to the snapshot state. | updateInPlace exists for exactly this in-place morph but is dormant, so scrubbing stays list-only. |
| Per-person history drawer on click | M | 4 | Clicking an AnalyticsPanel name filters history.actions for that participant id (Add, UpdateWeight, Spin, Remove) into a slide-in drawer, reconstructing each weight via getSnapshot(beforeActionId). | Per-person history is requested but nothing isolates one name's full arc today. |
| Replay-driven trend scrubber | M | 4 | Tie AnalyticsPanel to HistoryTimeline's selectedIndex so as j/k scrubs snapshots via getSnapshot, the odds bars recompute and animate to that point in time. | Lets a host walk the whole odds evolution using the history nav already built. |
| Inertial coast after manual drag-spin | M | 4 | After a grab-and-hold drag releases, feed the sampled angular velocity into a friction integrator on currentRotation that reuses segmentIdAtRotation and playTick per cross, decaying to rest and firing playThunk on settle. | Momentum carries the felt weight of the wheel and reuses the existing tick and thunk machinery for free. |
| Remote spin claim to prevent double spins | M | 4 | Two clients can both POST spin since doSpin only sets local suppressWsSpin; broadcast a SpinClaimed lock the moment any client presses, gating useSpin.handleSpinStart on every other client until SpinResult or a timeout clears it. | Stops the race where two people spin near-simultaneously and the room sees two conflicting winners. |
| Spin-locked orbit easing back | M | 4 | playSpin sets controls.enabled=false instantly; instead capture the current orbit pose and ease azimuth/polar back toward the hero shot during wind-up before locking so every spin launches from a clean frame. | Spinning from a user-orbited angle reads as broken; gliding to a canonical frame first keeps every spin composed. |
| Launch impulse scales with hold time | M | 4 | playSpin always uses fixed full turns; capture pointerdown-to-up duration on the canvas (onCanvasClick path) and map a longer press to more turns and longer dur so the user charges the spin. | A press-and-hold charge turns a binary click into a skill-feeling input with agency over spin intensity. |
| Count prefix queues repeated spins | M | 4 | In useHotkeys, buffer leading digits before Space the way vi buffers counts: '3' then Space queues three sequential handleSpin calls (each awaiting the prior reveal), with the pending count shown as a '3x' chip. | Bracket-style elimination needs several spins in a row, and a count prefix is the terse keyboard idiom for repetition. |
| Dot to repeat last command | M | 4 | Record the last dispatched command from CommandPalette's runActive and useHotkeys into a lastCommand ref, then bind '.' (when no editable target is focused) to re-run it via the existing run closure. | The vi dot-repeat is a high-leverage keyboard idiom and runnable commands are already centralized. |
| Pre-spin charge buzz on Space hold | M | 4 | Make Space in useHotkeys hold-aware: while held, run a rising navigator.vibrate pulse train and pass a charge value into playSpin so a longer hold seeds more rotations, released on keyup. | Turns spinning into a felt power-charge gesture where longer holds give longer dramatic spins. |
| Context group for current spin state | M | 4 | useCommandPalette builds a static command list; add a computed group reading ui.spinning and ui.winnerId so mid-spin it shows a Stop-unavailable note and after a win it surfaces Eliminate winner and Spin again. | The palette should offer what the moment calls for, not the same static list mid-spin. |
| Esc pops only the topmost layer | M | 4 | useHotkeys handles Escape with a flat if-chain that can dismiss the winner while the palette is open; replace it with an explicit modal stack (palette, recents, shortcuts, recap, winner, leader window) so Esc pops exactly the topmost layer. | Predictable single-step Escape is the backbone of modal keyboard UIs and the current chain ordering is fragile. |
| u and Ctrl-R as undo and redo | M | 4 | useHotkeys wires Cmd-Z to history.undo with a stubbed empty redo branch; add bare u (undo) and Ctrl-R (redo) when no editable target is focused, completing the redo path through useHistory. | The redo branch is an empty comment today, and the vi u/Ctrl-R pair is muscle memory for the keyboard-first audience. |
| Scoping sigils switch command source | M | 4 | In CommandPalette filtered(), detect a leading sigil in query (> actions, @ participants, ~ history snapshots) and filter props.commands by group before fuzzyScore runs, instead of searching the merged list. | With dozens of Eliminate rows, a sigil jumps straight to one command class without label noise. |
| Named camera shot presets switcher | M | 4 | Add a shots map (Hero front, Top-down high polar, Three-quarter) and a method that tweens camera.position and controls.target between them, reusing the polar clamps already set. | Curated angles let presenters frame the wheel for camera or projector without fumbling orbit by hand. |
| Drag handle for touch reorder | L | 4 | NameList reorder uses draggable="true" with HTML5 dragstart/drop which does not fire on touch, so add a grip affordance driving a Pointer Events reorder that maps pointer y to virtualSlice indices. | The current drag-reorder is silently broken on every touch device. |
| Relative reweight that holds others' shares | L | 4 | Offer an InspectorPanel mode where editing one weight rescales all other weights so their relative proportions stay fixed and only the edited name's share moves, solving for the multiplier and applying updateWeight across the set. | Bumping one name normally shifts everyone's odds, and this isolates the change to the name you touched. |
| Spin abort with a hand-brake drag | L | 4 | isSpinAnimating disables controls fully; allow a pointer-hold during the spin to apply extra deceleration (shorten remaining dur and add friction to eased) so the user can slam the brakes mid-spin. | Grabbing the spinning wheel is a core tactile fantasy and the frame loop already owns the easing it would modify. |
| Drag a segment outward to eject it | L | 4 | Raycast the segment under pointerdown and, on a radial outward drag past WHEEL_RADIUS, animate that mesh flying off (mirroring NameList's eject transform) then call remove; a short inward tug cancels. | A throw-it-off gesture matches the wheel-of-shame elimination metaphor literally. |
| Pour weight between two names | L | 4 | A pour gesture in InspectorPanel: drag from one name's token to another to transfer weight, decrementing source and incrementing target equally via paired updateWeight calls, keeping total bias constant. | Conserving total weight while shifting it frames reweighting as a zero-sum reallocation, matching how people think about odds. |
| Two-pane preview for history rows | L | 4 | useHistory already exposes preview(actionId); on a history command's active-row change (debounced) call it to show that snapshot's roster on the wheel behind the dimmed overlay, committing only on Enter via restoreTo. | Previewing a restore point before committing prevents blind time-travel mistakes. |
| Drag-and-release wind-up spin | L | 4 | playSpin already has a wind anticipation squash (wind = -dir*0.1, scale 0.97); let a touch-drag on the wheel rotate currentRotation live and feed the held-back angle as the wind value on release, with stepped vibrate buzzes selling stored tension. | Turns the existing canned wind-up into a tactile spring load the player controls, deepening the slot-machine feel. |
| Escape clears query before closing | S | 3 | In onKeydown the Escape branch closes immediately, so when query.value is non-empty clear it and reset activeIndex first, making one Escape reset the search and a second dismiss. | Users expect Escape to back out one level, not nuke the whole palette on a typo. |
| Auto-focus name input post-create | S | 3 | After useCreateSession.createSession resolves for a non-template session, call nameListRef.focusInput() so the cursor lands in the empty roster's name field. | The natural next action after creating an empty session is typing a name; focus should already be there. |
| Keyboard arrows adjust selected weight | S | 3 | Extend useHotkeys so Up/Down adjust the inspectorSelected weight by 0.1 (Shift for 1.0) via the existing adjustWeight path while InspectorPanel is focused, mirroring the +/- buttons. | Keyboard tuning fits the app's hotkey-first ethos and avoids precise mouse work on a thin track. |
| Double-tap to reset camera | S | 3 | resetView recenters the camera and is currently only reachable via the reset-pill on cameraDrifted; bind a double-tap on the canvas to call it so a fumbled orbit snaps back. | The reset pill is a hard tap target, and double-tap-to-reset is a learned map-app convention. |
| Native share sheet via navigator.share | S | 3 | copyLink and copyRecapImage both go through navigator.clipboard only; on supporting devices route the deep link plus the trophy PNG Blob through navigator.share({files}). | On phones the share sheet is the expected path while clipboard is a dead end. |
| Hold to ramp weight continuously | S | 3 | Make the InspectorPanel +/- buttons accelerate on hold: pointerdown starts a rAF loop calling adjustWeight with a growing delta until pointerup, instead of one 0.5 step per click. | Reaching 10x from 1x currently needs 18 clicks, and press-and-hold ramping is the standard fix. |
| Double-tap wheel to reset view | S | 3 | resetView() is only reachable via the drift pill; detect a double pointerdown within ~300ms on renderer.domElement when not spinning and call resetView with a smooth camera tween back to homeCamZ. | A quick gesture to recenter is faster than hunting for the reset pill after orbiting. |
| Filter history by action kind | S | 3 | HistoryTimeline visibleActions is an unfiltered reverse slice; add kind toggles (Spin / Weight / Add-Remove) that filter the listbox so a long run can be read as just its eliminations. | After many mixed actions the spin sequence drowns in weight tweaks, hiding the elimination story. |
| Tab to drill into a command | S | 3 | In CommandPalette onKeydown, treat Tab on a command that has inline config the same as Enter (enter inline mode) rather than only Enter, matching Raycast where Tab drills and Enter confirms. | Separating drill from confirm prevents accidental runs when the user only wanted to parameterize. |
| Direction alternates each spin | S | 3 | spinDirection is a fixed field defaulting to 1 and never flipped; toggle it each playSpin so consecutive draws reverse, with wind, overshoot, and target-delta math already keyed off dir. | Alternating spin direction breaks monotony cheaply and the entire physics path already respects the dir sign. |
| Shift-drag slider for fine 0.01 steps | S | 3 | In InspectorPanel onWeightInput, when the event has shiftKey, quantize to 0.01 and scale the applied delta down so the same pixel travel covers a tenth of the range before previewWeight fires. | The fixed step=0.1 makes dialing subtle bias near 1x impossible; a precision modifier fixes that without a second control. |
| Bracket keys reorder the focused row | S | 3 | With a roster row focused (focusedId), bind '[' and ']' in useHotkeys to roster.reorder(index, index-/+1), reusing the existing drag-reorder path, with an aria-live confirming the new position. | Drag reorder is mouse-only today; bracket-nudge gives keyboard users the same control over wheel order. |
| Flick the winner card to dismiss | S | 3 | dismissWinner is Escape or click today; on the winner overlay add a pointer fling that translates the card off-screen past a velocity threshold and calls dismissWinner, with a short drag snapping back. | Flicking the result away is a satisfying, momentum-aware close that matches the eject motion language. |
| Glow pulse on the hovered segment | S | 3 | Extend setHighlight (currently only z-lifts 0.18) to fade in a soft additive rim glow around the highlighted segment, driven by hoverPeekId, removed when id is null. | The z-lift is easy to miss on a busy wheel; a glow makes the hovered name unambiguous and reuses the existing highlight path. |
| Step undo by exactly one action | S | 3 | Cmd-Z in useHotkeys jumps to a whole snapshot; add bracket keys that step the HistoryTimeline selection by exactly one action and commit, for surgical single-step undo. | Snapshot undo is coarse; sometimes you want to peel off just the last weight change, not jump a snapshot back. |
| Scrub the weight readout numerically | S | 3 | Turn InspectorPanel's '{{weight.toFixed(1)}}x' header into a horizontal-scrub field (pointermove delta to weight), reusing the same 0.1-10 clamp and routing through previewWeight/updateWeight. | A draggable number gives fine control without the range track's coarse pixel-to-value ratio at the extremes. |
| Set focused weight with Shift plus digit | S | 3 | In useHotkeys, when a row is focused, Shift+1 through Shift+9 commit that integer weight via roster.updateWeight and Shift+0 resets to 1, with a fading delta chip. | Typing an exact bias is faster than arrow-nudging from 1.0 to 5.0, and the integer row is a natural keypad. |
| Tap the canvas anywhere to spin | S | 3 | onCanvasClick derives a left/right direction from clientX and only fires onSpinClick; on touch, treat a clean short tap as a plain handleSpin so the whole wheel is one big spin target. | New users instinctively tap the wheel, and the left/right split is an invisible distinction that adds no value on touch. |
| Optimistic restore with rollback | S | 3 | restoreTo in useHistory.ts awaits getSnapshot then setParticipants then a fire-and-forget restoreFromSnapshot.catch(()=>{}) that swallows failures; apply optimistically but roll the roster back if the server call rejects. | A swallowed restore failure lets the UI drift from the backend without the user knowing. |
| Confirm step for reset command | S | 3 | The palette 'reset' command's run() calls deps.reset() directly on a fuzzy match; swap it for an inline confirm row ('Type yes to restore N people') using the existing CommandInline mode so a full-roster reset is not one stray Enter away. | Reset wipes all eliminations and currently fires instantly from a single match. |
| gg and G to jump roster ends | S | 3 | useHotkeys cycles focus only via Tab; add 'gg' to set focusedId to the first active participant and Shift+G to the last, reusing the existing focusedId path and the focus aria-live announce. | On a 40-name wheel, jumping to the first or last name is constant and Tab-cycling there is slow. |
| Double-click weight slider to reset to 1x | S | 3 | InspectorPanel's range input handles input, change, and pointerup but has no dblclick reset; add an @dblclick that emits update-weight with 1 and flashes the header x readout. | Returning one name to fair is currently a multi-click drag back to center, and a double-click is the expected affordance. |
| Prevent-repeat toggle surfaces last pick | S | 3 | Add a palette command bound to roster.togglePreventRepeat whose subtitle names roster.lastPickedId's participant, so the rule's effect is legible. | Prevent-repeat is invisible state that changes odds, and the palette is the right place to expose and flip it. |
| Searchable shortcuts sheet | S | 3 | Add a filter input atop ShortcutsSheet.vue that fuzzy-matches labels and keys using the same fuzzyScore function already in CommandPalette.vue. | Once the keymap grows past a dozen rows a static list is slow to scan, and the matcher already exists. |
| Hover a name to isolate its odds arc | S | 3 | NameList emits hover-name and the renderer's setHighlight lifts that segment; also dim every other odds-donut arc's emissive so the hovered name's probability slice is isolated. | It reuses the existing hover-lift channel to let a host visually scope one participant's odds out of the crowd. |
| Palette Tab-completes highlighted command | S | 3 | In CommandPalette onKeydown, make Tab fill the query input with the activeCommand label (entering inline mode for inline commands) instead of leaving the modal. | Shell-style Tab completion lets users refine a fuzzy match into an exact one before committing. |
| Run keeps palette open with modifier | S | 3 | In runActive, when the Enter carried metaKey treat run()'s return as false so the palette stays open for chained actions like eliminating several names. | Power users doing a batch want to stay put; Cmd-Enter is the conventional stay-open chord. |
| Smooth mute with gain fade | S | 3 | toggleMute flips isMuted instantly so sounds cut mid-spin; instead ramp masterGain to 0 over 80ms and gate scheduling so muting during a spin fades rather than chops. | An abrupt silence mid-drumroll is jarring; a short fade feels intentional. |
| Two-finger spin direction on launch | S | 3 | onCanvasClick already derives left/right from clientX (line 644) and routes to onSpinClick; use that side to set spinDirection before playSpin so clicking the left half spins counterclockwise. | Letting the click side choose direction turns an existing dead heuristic into an intuitive physical control. |
| Inline add row below seeded names | S | 3 | After a template or sample seeds the roster, render a persistent ghost '+ add name' row at the bottom of NameList that focuses the name-input, so growing a seeded roster doesn't require scrolling back up. | Once names exist the top input is out of view on long lists; an inline add keeps momentum. |
| Drag weight slider with pointer fine-lock | M | 3 | InspectorPanel range emits preview on input and commits on pointerup, so on pointerdown capture the pointer and apply a vertical-distance precision mode (far from the track gives 0.01 steps) before the 0.1 commit. | Range inputs are coarse, and pointer-distance precision gives surgical tuning the 0.1 step blocks. |
| Dynamic look-at toward landing arc | M | 3 | During the SLOWMO stretch ease controls.target a small amount off-origin toward the segmentIdAtRotation winner's mid-angle world point so the frame anticipates where the pointer will stop. | Leading the composition toward the landing zone makes the result feel foreseen rather than arbitrary. |
| Flick history list to time-travel | M | 3 | HistoryTimeline scrub is mousedown/mouseup with j/k over .timeline-list, so add a vertical flick that throws selectedIndex through snapshots with inertia, each crossed item firing the existing preview emit. | Flicking through history is faster than clicking and reuses the per-action preview already wired. |
| Lock a weight against bulk edits | M | 3 | Add a lock toggle stored on Participant so that when bulk reweight operations are introduced (equalize, preset) a pinned weight is skipped while the others are rewritten through updateWeight. | Once one name is dialed in, future global operations should not clobber it, the main friction with any bulk reweight. |
| Parallax tilt from pointer | M | 3 | onCanvasMouseMove is a near no-op, so when idle offset camera.position by a tiny pointer-mapped vector and lookAt origin, giving subtle parallax without engaging OrbitControls. | Light pointer parallax adds depth to the idle hero shot while leaving the orbit gesture for real navigation. |
| Pen pressure scales spin force | M | 3 | In the pointerdown handler on renderer.domElement read PointerEvent.pressure when pointerType is pen and map stylus pressure to playSpin's rotation count and SLOWMO drama. | Stylus pressure is an expressive input the app ignores entirely today. |

### Layout (23)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Upright-flip labels past six o'clock | M | 5 | Labels set rotation.z=startAngle+segAngle*0.5 with no hemisphere correction (ThreeWheelRenderer.ts:415), so bottom-half slices render upside-down; add Math.PI plus a radial offset when the mid-angle crosses into the lower hemisphere. | Half the wheel shows inverted text, the single most jarring readability defect. |
| Promote spin to a thumb-sized FAB | S | 4 | App.vue exposes dock-spin as a small glass button disabled while spinning; on mobile promote it to a circular FAB pinned bottom-right above the roster sheet, bound to the same handleSpin path. | The primary action deserves a thumb target rather than a 12px chip shared with secondary controls. |
| Aspect-aware portrait framing | S | 4 | In onResize, when aspect < 1 increase camera.position.z and lower fov so the round wheel fits the tall viewport, distinct from the single BASE_CAM_Z used today. | One fixed camera distance crops or wastes space on phones; aspect-aware distance keeps the wheel centered and whole. |
| Inline create form, skip blank screen | M | 4 | Replace App's full-page .create-screen gate (Create a session to get started) with the wheel already visible behind a compact floating create card so the 3D scene and WheelSkeleton ring are the first thing seen. | Leading with the product's signature visual sells it harder than a centered prompt line. |
| Weight histogram strip in inspector | M | 4 | Add a compact bar row above the InspectorPanel slider plotting every active weight from activeParticipants with the selected name highlighted, so you scrub against the whole distribution. | Bias is relative, and seeing where this name sits among the others makes the right slider position obvious. |
| Move dock to thumb zone | M | 4 | The mobile media query parks .action-dock at bottom:calc(50vh+12px) centered, stranding 16+ buttons mid-screen; collapse it to a thumb-reachable bottom bar with Spin as a primary FAB and the rest behind an overflow. | Sixteen tiny glass buttons floating at mid-height are unreachable one-handed and overlap the wheel. |
| Radial auto-fit font size to slice width | M | 4 | makeSimpleLabel always uses bold 28px on a 1.6-wide plane regardless of p.angle; measure the chord at WHEEL_RADIUS for each segment and shrink font and plane width so narrow weighted slices don't overflow neighbors. | Weighted slices vary in angle, so one fixed label width spills across dividers on thin segments. |
| Frame-to-fit on roster change | M | 4 | fitWheel() only recenters wheelGroup and ignores size; compute homeCamZ from segment count and aspect so a 60-name wheel pulls back and a 4-name wheel pushes in, calling it from build() and onResize. | Keeps the wheel well-composed across very different rosters instead of a fixed distance. |
| Word-wrap two-line labels for long names | M | 4 | makeSimpleLabel does single-line fillText with a hard slice(0,17) plus ellipsis; for names containing a space, wrap onto two baselines within the 64px-tall canvas before falling back to ellipsis. | Full first-and-last names read far better stacked than chopped at 17 characters. |
| Initials-only label below angle threshold | M | 4 | When a segment's p.angle is too small for even a condensed name, have makeSimpleLabel render just the participant initials at larger size, reserving the full name for the inspector and peek path. | On crowded wheels large initials are the only honestly readable option, beating a three-character ellipsis stub. |
| Partial billboard tilt on labels | L | 4 | Labels are parented to the segment with only rotation.z; add a per-frame quaternion nudge yawing each label toward camera.position so glyphs stay readable when OrbitControls tilts past minPolarAngle 0.3. | On a steeply tilted wheel the flat planes foreshorten to slivers; partial billboarding keeps names face-on without losing depth. |
| Baseline-shift labels off inner rim | S | 3 | Labels sit at segment center while build draws an inner TorusGeometry at WHEEL_INNER 0.6 and an odds donut at DONUT_INNER..OUTER, so offset each label radially outward to a clear band to avoid the hub and ring. | Centered labels can overlap the inner rim and odds donut on short names, muddying both. |
| Identity-token mini avatars in stats | S | 3 | Replace AnalyticsPanel's plain name spans with the round identityColor token and initial used in RecapReel's reel-token so stat rows match the roster and recap visuals. | Unifies the visual language so a name reads the same in roster, recap, and stats. |
| Safe-area insets for notch | S | 3 | The App overlay uses padding:20px and the dock uses fixed bottom offsets with no safe-area handling and the viewport meta has no viewport-fit, so add viewport-fit=cover and env(safe-area-inset-*). | On notched phones the title and bottom dock currently collide with the system UI. |
| Dock divider tokens group controls | S | 3 | The action-dock packs many flat buttons with uniform gap and no grouping; add a --dock-divider token rendering hairline separators between spin, export, theme, and editor clusters. | A long flat bar is hard to scan; token-driven dividers chunk it into readable groups. |
| Recent commands cap and pin | S | 3 | In filtered() empty-query branch, cap the frecency-lifted Recent group to the top 3 by frecencyScore and keep the rest in declared order so Recent stays a glanceable shortlist. | An unbounded Recent group reorders the whole list and hurts spatial memory. |
| Weight badge on each NameList row | S | 3 | Add a small 'Nx' chip next to .odds-pct in NameList for any row whose weight differs from 1, tinted with identityColor, so biased names are scannable without opening the inspector. | Bias is invisible in the list today; a chip surfaces which names are weighted at a glance. |
| Effective sample size readout | S | 3 | From preparedSegments weights compute Kish effective N = (sum w)^2 / sum(w^2) and show it in AnalyticsPanel, so 8 heavily-weighted names reads as an effective field of 4.2. | One number captures how much the weighting has shrunk the real field of contenders. |
| Per-row mass shadow under heavy names | S | 3 | In NameList, scale each row's box-shadow blur and offset by its effective weight (via getOdds) so a 5x name casts a deeper drop shadow and reads as physically heavier in the stack. | Makes weight a felt-by-eye property of the list itself, reinforcing the tactile metaphor with no device support needed. |
| Empty-query shows next-likely action | M | 3 | In rows(), when query is empty and not spinning, pin a single Suggested group above Recent containing one command chosen from ui state (spin if roster full, reset if all removed). | A confident single suggestion beats scanning Recent when intent is obvious from state. |
| Label opacity fade on far-side segments | M | 3 | Labels use DoubleSide planes so back-facing names show mirror-reversed under the tilted camera, so fade label material.opacity by the dot of the label normal and camera direction so reversed text recedes. | DoubleSide currently shows readable-but-mirrored names on the far rim, which fading removes. |
| Orientation-aware layout switch | M | 3 | The single max-width:700px query makes .list-section a bottom sheet regardless of orientation, so add an orientation:landscape branch returning the roster to a right rail. | A landscape phone has the aspect ratio of a small desktop, so a half-height bottom sheet wastes the side space. |
| Phase strip: Setup / Live / Closed | M | 3 | Add a thin phase indicator in the App header driven by uiStore: Setup (roster editable), Live (spinning disables roster edits), Closed (one survivor), each phase gating which controls are enabled. | Makes the ritual's stage legible, preventing mid-spin roster edits and clarifying when the run is over. |

### Accessibility (42)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Assertive winner announcement region | S | 5 | App.vue has a single polite aria-live for focusAnnounce and onWinnerReveal speaks nothing, so the pick outcome is silent; add an aria-live=assertive region fed from spin.onWinnerReveal reading 'Picked {name}, {remaining} remaining'. | The single most important event in the app is currently never spoken to screen readers. |
| Contrast-aware label ink per segment | S | 4 | makeSimpleLabel hardcodes ctx.fillStyle '#000' over identityColor's hsl(h,62%,66%) fills; compute the segment color's relative luminance and pick black or near-white ink so darker hues stay legible. | Black on a 66%-lightness pastel is fine but fails on the darker hues in the 360-hue space. |
| Reduced-motion locked camera | S | 4 | Gate all new camera moves (dolly, shake, FOV) behind the existing prefersReducedMotion check used for bloom and wind-up, keeping the camera at home and skipping tweens for those users. | Camera moves are a top vestibular trigger; honoring the existing reduced-motion path prevents nausea while keeping the spin functional. |
| Contrast-checked label text on segments | S | 4 | makeSimpleLabel always paints name text in #000 regardless of segment lightness; compute relative luminance of the seg color and choose black or white ink so labels stay legible on dark hues. | Names on darker auto-generated segment colors fall below contrast and become unreadable. |
| Inspector spinbutton arrow grammar | S | 4 | The InspectorPanel weight control has +/- buttons that step 0.5 but no keyboard parity; give it role spinbutton with ArrowUp/Down=0.5, PageUp/Down=1.0, Home/End=min/max, each firing the existing update-weight emit and updating aria-valuetext. | The buttons and arrows should agree, and spinbutton semantics make the weight control keyboard-complete. |
| Announce the active mode for screen readers | S | 4 | When a modal key state engages (leader armed, visual selection, count buffer), write a terse message to the existing polite aria-live region ('chord g', 'count 3', 'selection 4 names') so the keyboard grammar is audible, not just visual in the HUD. | Modal keyboard states are invisible to screen-reader users unless explicitly announced. |
| Mute toggle also silences vibration | S | 4 | Route every navigator.vibrate call through a guard that respects the existing isMuted flag (wheel-muted localStorage) so one toggle silences both audio and haptics. | Haptics can be as intrusive as sound in a meeting; tying them to the existing mute keeps a single expected off switch. |
| Reduced-motion calm particle fallback | S | 4 | Gate every new 3D emitter behind the existing prefersReducedMotion() check in ThreeWheelRenderer, swapping bursts for a single brief static emissive bloom on the winner donut, matching how useFlame and playSpin already branch. | New VFX must honor the same reduced-motion contract the renderer already follows to stay accessible. |
| Reduced-motion disables all haptics | S | 4 | Gate every navigator.vibrate and gamepad rumble call behind the renderer's existing prefersReducedMotion() check, the same guard that already skips bloom, wind-up, and hitstop. | Vestibular and sensory sensitivities extend to vibration, so haptics should not bypass the user's stated preference. |
| Roster odds exposed to screen readers | S | 4 | In NameList the odds-pct span is aria-hidden and getOdds is never spoken; add an SR-only suffix per row naming the name, percent chance, and rank. | The odds, the core decision data, are deliberately hidden from assistive tech right now. |
| Spin start and settle narration | S | 4 | handleSpin and ThreeWheelRenderer.playSpin's settle() emit no SR output; announce 'Spinning N names' on start and a polite 'Wheel stopped' on the settle onTick(null). | Without start and stop cues the wheel is an opaque silent box between Space and the result. |
| Focusable canvas with aria description | S | 4 | WheelCanvas mounts a bare three.js domElement with no role, tabindex or label; give the container role=img with an aria-label summarizing 'Wheel with {n} names, {topName} most likely', rebuilt alongside roster.preparedSegments. | The central visualization is currently invisible to assistive tech as a single unlabeled region. |
| Aria-live narration on history scrub | S | 4 | HistoryTimeline selectAction only emits preview; also push the snapshot delta into the existing polite aria-live so screen-reader users hear 'restored to: 8 active, 3 eliminated' while roving j/k. | The scrub is purely visual today, so keyboard-only and screen-reader users get no feedback on where they are. |
| Auto-derive readable text on accent | M | 4 | dock-spin and btn-primary pair fixed text colors directly on accent fills (btn-primary uses #2d3436 on #4ECDC4), so when --accent becomes user-picked derive --on-accent through a luminance test instead. | The moment the theme picker works, fixed text on a variable fill will fail contrast on dark accents. |
| Tab order escape from canvas trap | M | 4 | useHotkeys intercepts Tab to cycle wheel focus whenever not in an input and the roster has names, so Tab never reaches dock or roster controls; limit the Tab-capture to when the canvas region itself holds focus. | Hijacking global Tab breaks the expected keyboard traversal of every other control. |
| Tiered reduced-motion control | M | 4 | ThreeWheelRenderer.prefersReducedMotion is a single OS gate flipping bloom off and spin to 520ms; add a three-tier ui store setting (full/reduced/none) so users can keep a short spin but kill bloom, or skip animation entirely. | OS reduced-motion is all-or-nothing, but vestibular users often want a calm spin rather than an instant jump. |
| Self-revealing key hints on hover | M | 4 | Add data-key attributes to the Spin button, add field, and panel toggles in App.vue plus a delegated handler that after about 500ms hover fades a kbd chip near the control reading its bound key, exposing existing shortcuts without adding any. | ShortcutsSheet is hidden behind '?'; surfacing the key on the control itself teaches the grammar passively. |
| Winner modal as focus-trapped dialog | M | 4 | SpinResult is a plain div with no role=dialog, aria-modal, autofocus, or focus trap; add role=dialog, aria-labelledby the h2, autofocus the Continue button, and restore focus on close. | The result overlay is currently unreachable and unannounced for keyboard and SR users. |
| High-contrast skin fills token gaps | M | 4 | .high-contrast in style.css only sets --bg/--fg and a kbd and focus-ring override, leaving glass panels translucent grey and accent teal; extend it to force opaque panels, a vivid accent, and no blur. | The current high-contrast mode changes almost nothing on the actual gameplay surfaces. |
| Honor reduced-transparency on glass surfaces | M | 4 | Every panel, dock, and toast leans on backdrop-filter blur over translucent backgrounds; under @media (prefers-reduced-transparency: reduce) swap to opaque backgrounds and drop the blur, mirroring the existing reduced-motion handling. | Backdrop blur over the bright wheel hurts text legibility for low-vision users and costs perf. |
| Trap focus inside open dialogs | M | 4 | The palette and ShortcutsSheet are role dialog aria-modal but nothing constrains Tab, so focus escapes behind; add a first/last sentinel focus trap keyed off the open prop and restore focus on close. | A modal that leaks focus breaks keyboard and screen-reader users, and the dialogs already claim aria-modal. |
| Color-blind safe segment patterns | L | 4 | identityColor maps name hash to hue only (62%/66%), so adjacent segments can be near-identical for deuteranopia; derive a per-name pattern (solid/stripe/dot) from the same hashName and apply it as a canvas-texture overlay in build. | Hue alone fails about 8% of men; patterns make segments distinguishable without color. |
| Describe odds bar fill for SR | S | 3 | NameList renders the per-row probability as a CSS ::before width var(--odds-width) with the odds-pct marked aria-hidden, so add role=progressbar with aria-valuenow=odds on the row's odds region. | The visible probability bar conveys nothing to assistive tech. |
| Diacritic-safe baseline for labels | S | 3 | makeSimpleLabel fillText at y=40 on a 64px canvas clips tall accents and descenders, so compute ascent/descent from ctx.measureText and center the baseline so names like Jurgen or Lukasz are not cropped. | The fixed y=40 baseline silently truncates diacritics, corrupting non-English names. |
| Lightness-varied identity palette | S | 3 | identityColor fixes saturation/lightness at 62%/66% and varies only hue, so fold a second hash byte into lightness (58-74%) and saturation so adjacent segments differ on a second axis. | Adds a non-hue distinguishing dimension cheaply, helping low-vision and color-blind users. |
| Reduced-motion gets a dignified ease-out | S | 3 | The reduced branch uses linear eased=t over dur=520 with no overshoot, so swap to an ease-out cubic and a tiny scale settle so reduced motion still lands deliberately. | Linear-to-hard-stop reads as a glitch, while an ease-out respects the preference and stays legible. |
| Skip-to-roster and skip-to-wheel links | S | 3 | App.vue has no landmark skip links and Tab is hijacked for wheel focus cycling; add visually-hidden skip links (reusing .visually-hidden) that jump focus to the canvas and the NameList input. | Keyboard users must tab through the whole dock to reach the roster or wheel. |
| Honor prefers-contrast for theme | S | 3 | ui.toggleTheme flips .high-contrast only by manual button; add a matchMedia('(prefers-contrast: more)') watcher that seeds currentTheme and the document className on mount, mirroring the existing reduced-motion read. | Users who set OS high-contrast should not have to rediscover and click a dock button. |
| Landmark roles for overlay regions | S | 3 | App.vue uses bare divs for .session-header, .action-dock, and .list-section; mark the dock as role=toolbar aria-label='Wheel actions' and the list-section as an aria-labelled complementary region. | Screen reader rotor navigation has no landmarks to jump between the three main areas. |
| Respect OS reduce-motion for fire title | S | 3 | The .fire-text gradient runs an infinite fireShift animation and useFlame's ember canvas loops, and the reduced-motion blocks only drop transitions; pause the title shimmer and ember loop under the media query. | A perpetually animating title is a known migraine and vestibular trigger ignored today. |
| Calmer audio profile under reduced motion | S | 3 | prefersReducedMotion already shortcuts dur to 520 and skips hitstop; under the same flag, gate ticks to a single soft confirmation tone and a gentle thunk instead of the full ramp. | Users who reduce motion often want reduced sensory intensity too, so it respects the same intent. |
| Announce the last one standing | S | 3 | NameList computes survivorReached and shows a Survivor chip plus gold trim, but the milestone is never spoken; announce 'Last one standing: {name}' via the polite live region when active.length hits 1. | The endgame moment is purely visual and missed by screen-reader users. |
| Live region for roster size changes | S | 3 | addName, removeName and handleRemove mutate the roster but only push visual toasts; announce 'Added {name}, {count} on the wheel' and 'Removed {name}' through a polite aria-live region driven by roster events. | Blind users lose track of who is on the wheel as names are added and ejected. |
| Mono grayscale skin for projectors | S | 3 | identityColor drives wheel segments and roster swatches off hue; a mono skin token forcing the identity saturation to ~8% yields a high-legibility grayscale wheel, distinct from the pure-black high-contrast mode. | Pastel hues blow out on washed-out projectors in bright rooms; a desaturated skin stays readable. |
| Locale-aware label font fallback | S | 3 | makeSimpleLabel sets the canvas font to 'bold 28px Roboto, sans-serif' while the opentype font loaded is Roboto-Bold (Latin only); add a CJK and Cyrillic system fallback to the canvas font string. | Roster names are user-entered and may be non-Latin, and the canvas path can fall back even where the opentype path cannot. |
| Ctrl-N and Ctrl-P navigate the palette | S | 3 | CommandPalette.vue's onKeydown only moves activeIndex on ArrowDown and ArrowUp; add Ctrl-N/Ctrl-P and Ctrl-J/Ctrl-K aliases so home-row users never leave it. | Arrow-only navigation forces a hand move and is inconsistent with the j/k grammar in HistoryTimeline. |
| Pause aria-live spam during fast spins | S | 3 | onTick fires per segment cross and drives tickingId; suppress any live-region announcements while spinning.value is true and emit one summary at settle. | Per-tick announcements would make the spin an unusable wall of speech for screen-reader users. |
| Reduced-motion drain for undo timer | S | 3 | ToastStack hides .toast-timer entirely under prefers-reduced-motion, leaving the undo window invisible; show a static numeric countdown ('Undo (3s)') updated each second instead. | Reduced-motion users currently get no signal at all that the time-limited undo window is closing. |
| History timeline labels resolve names | S | 3 | HistoryTimeline formatAction truncates ids to 8 chars ('Spin: a1b2c3d4'); resolve participant ids to names for the role=option label so screen-reader users hear meaningful history instead of hashes. | The scrub list reads as opaque hashes to a screen reader, conveying nothing. |
| Localize voice recognition language | S | 3 | useVoice hardcodes rec.lang='ru-RU' (line 14), so English speakers using the voice add/spin buttons get poor recognition; read navigator.language or a ui setting to set lang while keeping the multi-language spin keyword match. | Voice input, an accessibility affordance for motor-limited users, only works well in Russian today. |
| Audio-only landing cue distinct from ticks | S | 3 | playTick and playThunk are the only non-visual landing feedback; add a distinct rising arpeggio at settle (separate from the 140Hz thunk) so audio-only users get an unmistakable landed signal versus mid-spin ticks. | Mid-spin ticks and the final thunk are too similar to reliably mark the result by ear. |
| Token-driven focus ring per theme | S | 3 | style.css ties :focus-visible outline to --accent and hardcodes a white ring under .high-contrast; generalize to a --focus-ring-color token each skin sets, instead of the two-branch special case. | With custom accents, a low-contrast accent silently makes the keyboard focus ring invisible. |

### Collaboration (27)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Real presence count over WebSocket | M | 5 | Replace the fake presence span in App.vue:668 (Math.floor(Math.random()*3)+2 peers) with a live count from a server presence_changed event, derived from the Hub receiver_count already tracked per session channel in ws.rs. | A wheel of shame is a group ritual and a fabricated peer count breaks trust the instant two people compare screens. |
| Resume action stream after reconnect | L | 5 | handle_ws silently continues on broadcast Lagged with no catch-up, so have the client send its last-seen seq on open and replay missed actions via listActions. | After a network drop a client currently desyncs permanently because lagged broadcasts are dropped with no recovery. |
| Live odds-change broadcast on weight | S | 4 | InspectorPanel weight edits already emit segment_updated over WS (handlers.rs:296); surface a peer-attributed toast 'Dana set Bob to 3x' and flash the affected NameList odds (getOdds, NameList.vue:58). | Silently changing someone's odds in a shared room needs witnesses, and the event already travels but lands invisibly. |
| Presence-aware aria-live announcements | S | 4 | Feed peer join/leave and remote-spin attribution into the existing single polite aria-live region (App.vue:586) so screen-reader users hear 'Dana joined' and 'Dana spun, Bob picked'. | Co-presence is invisible to non-sighted facilitators today and the one live region is the already-wired channel for it. |
| Calendar event creates prefilled session | M | 4 | Add a /api/v1/sessions/from-calendar endpoint that reads a calendar event's attendee list and calls create_session plus add_participants_batch, mapping attendee display names into Participant.name. | Removes manual roster typing for recurring standups that already live on a calendar invite. |
| GitHub reviewer assignment integration | M | 4 | A GitHub Action calls POST spin against a team session and writes SpinResult.picked.name into the PR's requested reviewers via the GitHub API. | Randomizes code-review load using the same fair-spin logic the team already trusts. |
| Live history append over WebSocket | M | 4 | ws.rs broadcasts SessionEvent::ActionLogged but the timeline only refreshes via history.loadActions, so have the App.vue WS handler call recordAction on receipt so remote spins stream into HistoryTimeline live. | Remote actions currently appear late or not at all because the timeline never consumes the live event. |
| Spectator overlay of incoming WS spins | M | 4 | SessionEvent::SpinResult already arrives over the WS subscription for all clients, so in a spectator view animate each broadcast result as a lower-third toast reusing SpinResult.vue. | A passive viewer link is only compelling if results visibly stream in. |
| Per-action version on WS broadcasts | M | 4 | SessionEvent variants carry no version; SpinResult and SegmentUpdated broadcast bare participants, so attach the action seq so clients detect gaps. | Lets roster.ts skip stale out-of-order messages and request catch-up instead of silently applying them. |
| Shared spotlight ping on a name | M | 4 | Add a click-to-ping that broadcasts spotlight {id,peer} and, on all clients, briefly pulses setHighlight plus a NameList row flash in the pinger's identityColor, auto-clearing after about 1.5s. | Gives facilitators a 'look at this one' gesture that lands on every screen, reusing the highlight path. |
| Spin lock to prevent double spins | M | 4 | Broadcast spin_arming then reject a concurrent spin in handlers.rs:175 while one is in flight, returning a 409 the client shows as 'Dana is spinning', cleared on the resulting spin_result broadcast. | Two people pressing Space within the animation window both call api.spin today; a short server-side lock keeps the elimination single and fair. |
| Peer hover lifts a phantom segment | M | 4 | Broadcast each client's hoverPeekId (ui.ts setHoverPeekId) over WS; when a peer hovers a name, call ThreeWheelRenderer.setHighlight for that id but tint the z-lift with the peer's identityColor. | Reuses the existing single-highlight z-lift to show that someone else is eyeing a name without any new 3D geometry. |
| Slack Block Kit spin result card | M | 4 | Reuse buildRecapSummary/buildPodium from utils/recap.ts to render a Slack Block Kit message (survivor plus medal podium) posted on each SpinResult broadcast in handlers::spin. | Turns the existing recap copy into a rich channel post so absent teammates see who got picked. |
| Spotlight follow on remote spin | M | 4 | When a spin_result arrives from another client (useSession, suppressWsSpin false) briefly ease the local OrbitControls camera toward home via resetView() so spectators snap to the same framing the spinner sees. | A remote spin can land while your camera is drifted off-axis so you miss the reveal; a gentle re-frame keeps everyone watching the same thing. |
| Embeddable read-only wheel iframe | M | 4 | Ship an /embed/{id} route mounting WheelCanvas in spectator mode: it opens useWebSocket, renders SpinResult events, and hides NameList and InspectorPanel editing controls. | Drops a live wheel into Notion, Confluence, or a wiki as an iframe with no edit affordances. |
| Optimistic concurrency on prop updates | M | 4 | update_participant_props in sqlite does a blind UPDATE inside begin_write with no version check, so two InspectorPanel weight edits last-write-win silently. | A version guard lets the server reject a stale weight write and tell the client to refetch instead of clobbering. |
| Co-watch winner reveal sync | M | 4 | On a remote spin_result, useSession only pushes a toast while the celebratory SpinResult modal fires only for the spinner's own spin (the suppressWsSpin path); gate the modal to open on every client at animation-complete using remoteSpinResult. | The climactic reveal is private to the spinner today, so broadcasting it makes the landing a shared beat. |
| Slack slash command spins the wheel | M | 4 | A new axum route /api/v1/sessions/{id}/slack mirrors handlers::spin then posts SpinResult.picked.name back via the Slack response_url, instead of only broadcasting SessionEvent::SpinResult. | Lets a standup host run the wheel from Slack without alt-tabbing to the browser. |
| Signed read-only spectator share link | L | 4 | useSession only knows the raw id for sharing, so generate a separate HMAC-signed spectator URL the WS upgrade and REST GET accept but the spin/delete/reset handlers reject. | Sharing a wheel for spectating currently forces handing over full edit power. |
| Read-only spectator deep link | L | 4 | The WS route broadcasts SpinResult to all clients; add a #/<id>/watch hash that subscribes read-only, hiding NameList editing and the spin dock so a shared link is safe to post publicly. | Hosts want to broadcast the draw without handing strangers the roster controls. |
| Live cursor ghosts on the wheel | L | 4 | handle_ws ignores all inbound client frames (the _ => {} arm); add a cursor_moved {x,y,peerId} message and render throttled peer dots over WheelCanvas colored via identityColor(peerName) so each cursor matches its owner's hue. | Seeing where others point turns a silent pre-spin moment into a shared one, and the inbound channel is currently dead. |
| Kick and ban a connected client | L | 4 | The Hub broadcasts to all subscribers with no per-client identity; tag each Subscription with a client token and add a host-triggered drop that closes a specific socket and refuses its token on reconnect. | A host has no way to remove a disruptive spectator from a live room. |
| Drift-aware recenter nudge for spectators | S | 3 | The onDrift callback already knows when a local camera wandered; when a remote spin is arming, prompt drifted spectators with a 'Recenter to watch' toast wired to resetView. | Combines the existing drift signal with spin arming so nobody misses the reveal because their orbit camera floated away. |
| Color-coded action log authorship | S | 3 | HistoryTimeline.vue renders Actions whose model already carries actor: Option (handlers.rs:277); render each entry's left rail in the actor's identityColor so the timeline reads as a multi-person diary. | The actor field is plumbed end to end but unused, so coloring it turns history into a who-did-what record at a glance. |
| Avatar stack in the header | S | 3 | Render a row of identityColor-tinted initial tokens (initialOf from NameList) in the fire-header for each connected peer, sourced from the presence event that replaces the random peer count. | A concrete who-is-here strip anchors other co-presence cues and gives the placeholder a real home. |
| Presence-gated reset confirmation | S | 3 | Before reset() broadcasts session_reset, if more than one peer is present show a confirm seeded by the real presence count from the WS subscriber tally. | Reset wipes everyone's eliminations, so knowing others are watching raises the bar before nuking shared state. |
| Confetti color drawn from full roster | S | 3 | Build the winner confetti palette by sampling identityColor over activeParticipants names from the roster store, not just the winner, so the celebration rains everyone's colors. | Ties the per-name identity-color system into the VFX so the group sees themselves in the winner's confetti. |

### Performance (26)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Cache and reuse label textures by name | M | 5 | makeSimpleLabel allocates a fresh 256x64 canvas plus CanvasTexture on every build() per name, and clearGroup never disposes them since labels carry no sharedMaterial flag, so each weight-drag rebuild leaks one texture per name to the GPU. Add a Map keyed by name caching the CanvasTexture and disposing evicted entries. | Stops a real per-rebuild GPU texture leak and skips redundant canvas rasterization on the hot weight-edit path. |
| Build a real 2D canvas renderer | L | 5 | WheelRenderer is the documented abstraction but only ThreeWheelRenderer exists; add a Canvas2D WheelRenderer drawing arcs (mirroring exportWheelSVG angle math) selected via capability or reduced-motion check in WheelCanvas.vue. | Gives low-power devices a path that never downloads the three chunk, the largest bundle and GPU cost. |
| DPR-scaled label canvas for crispness | S | 4 | makeSimpleLabel creates a fixed 256x64 canvas while the renderer clamps setPixelRatio to min(dpr,2); multiply the label canvas dimensions and font px by that factor so CanvasTexture text isn't blurry on retina or when zoomed. | Fixed-resolution label textures look soft on the high-DPI displays the renderer otherwise respects. |
| Remove unused opentype font load | S | 4 | loadFont runs opentype.load('/Roboto-Bold.ttf') and a throwaway build([]) in the constructor, but makeSimpleLabel renders with canvas 2D fonts and _otFont is only voided, so the fetch, parse, and opentype vendor chunk are pure mount-time cost for a font nothing draws with. | Removing a font download and parse from the canvas mount path speeds first paint and drops a bundle chunk. |
| Master gain bus with compressor | S | 4 | playTick and playThunk both connect oscillators straight to audioCtx.destination; insert a shared masterGain into a DynamicsCompressorNode into destination, built once in initAudio. | Stops the 0.25 thunk and stacked rapid ticks from clipping and gives one node for global volume and ducking. |
| Trail emitters release the dirty flag | S | 4 | The render loop only renders when needsRender is true; have every decaying particle emitter call markDirty() each active frame and stop requesting frames once all particles expire. | A particle system that forgets to release the dirty flag would pin the GPU at full rate indefinitely. |
| Half-resolution bloom pass | S | 4 | initScene builds UnrealBloomPass at full container resolution (line 240); construct its Vector2 at half width and height since bloom is a low-frequency glow that upscales cleanly, keeping RenderPass and OutputPass full-res. | Bloom is the most expensive composer pass and tolerates half-res with no visible quality loss, a constant per-frame saving. |
| Coalesce rapid builds per frame | S | 4 | WheelCanvas deep watcher can fire build() many times within a frame during a batch paste, since addNames pushes participants one by one; debounce build to one call per animation frame via a pending flag. | A 50-name paste triggers many full rebuilds in one tick; collapsing to the final state once per frame is a large bulk-add win. |
| Diff preparedSegments to pick build vs update | M | 4 | Add a shallow diff in WheelCanvas.vue's preparedSegments watcher comparing previous to new id lists: same ids and length calls updateInPlace, a changed id set calls build, replacing the blanket deep-watch to build() path. | Lets the renderer auto-pick the cheap path so callers do not have to know which mutation happened. |
| Drop pixel ratio during the spin | M | 4 | initScene fixes renderer and composer pixelRatio to min(dpr,2); in playSpin lower both to about 1.25 for the duration and restore on settle() so bloom-composited frames render fewer pixels while motion hides the softness. | The 3.2s spin is the only sustained 60fps stretch and the only place full-res bloom hurts, and softness is invisible on a blurring wheel. |
| Instance the peg meshes | M | 4 | build() pushes one THREE.Mesh per peg into pegMeshes sharing getPegGeo/getPegMat; replace with a single InstancedMesh sized to segment count, writing each peg's position into instanceMatrix. | For a 100-name roster this collapses 100 peg draw calls and 100 nodes into one, cutting build and per-frame cost. |
| Cache label textures by name | M | 4 | build calls makeSimpleLabel on every full rebuild, recreating canvases and CanvasTextures; key a cache by name+fontPx+color (mirroring cachedMaterials/segGeoCache) and reuse across rebuilds. | Full rebuilds happen on any roster change, so regenerating identical label textures wastes canvas work and GPU uploads. |
| Pause controls.update at rest | M | 4 | startRenderLoop calls controls.update() and renders every frame unconditionally; track whether OrbitControls damping is still settling (camera delta vs an epsilon) and skip both once idle, letting needsRender gate everything. | At rest the app re-runs damping math and a full composer render every frame for a static image, burning battery. |
| Pack labels into one texture atlas | L | 4 | Each name label is its own PlaneGeometry plus MeshBasicMaterial child of its segment mesh in build(); render all names into one atlas canvas and give each label plane a shared material with per-plane UV offsets. | Replaces N label materials and N textures with one bound texture, the dominant material count on large rosters. |
| SDF text instead of canvas textures | L | 4 | Replace makeSimpleLabel's per-name 256x64 CanvasTexture and PlaneGeometry with a signed-distance-field glyph atlas built from the loaded font, so labels stay crisp at any zoom and tilt with one shared atlas. | Resolution-independent text removes the blur-versus-memory tradeoff the fixed canvas forces. |
| Optimistic spin with server-confirmed correction | L | 4 | doSpin awaits api.spin before lastSpinResult is set; instead pick a provisional winner locally via roster.doPick, start playSpin immediately, and gently re-target if the authoritative SpinResult differs. | The wheel reacts on press instead of after the network, the single biggest perceived-latency win. |
| OffscreenCanvas worker for wheel render | L | 4 | ThreeWheelRenderer creates its WebGLRenderer on the main thread; where supported, transfer an OffscreenCanvas to a render worker (vite ?worker like wheel-math.worker) so scene init, font load and the spin loop run off-thread, degrading to the current path otherwise. | Moves the heaviest GL work off the main thread so roster edits and Vue reactivity stay responsive during a spin. |
| Index snapshot lookup by session and seq | S | 3 | sqlite get_snapshot runs ORDER BY rowid DESC LIMIT 1 over snapshots with no composite index on session_id, so add one to keep scrub and undo lookups from scanning as snapshots accumulate. | Scrub and undo lookups slow down as snapshots accumulate per session. |
| Skip labels past a count threshold | S | 3 | build() always calls makeSimpleLabel per participant, so past a threshold (e.g. >40 names) where labels are unreadable anyway, skip them and rely on the roster, cutting N canvas rasterizations and N planes. | On dense wheels labels add cost for text no one can read, so dropping them is a large build-time saving with no UX loss. |
| Fail the shrink bench on budget overrun | S | 3 | wheel-bench.mjs prints median ms/frame against the 16.7ms budget but never fails; make it exit non-zero when the after path exceeds budget so it can gate merges. | The benchmark measures the right thing but is purely informational, so a perf regression passes CI unnoticed. |
| Tab-visibility audio suspend | S | 3 | The renderer pauses rAF when the tab is hidden (per the Preview throttling note); add a visibilitychange handler that calls audioCtx.suspend() when hidden and resume() when visible. | Prevents scheduled ticks from queuing and firing in a burst when the tab refocuses. |
| Guarantee zero frames at rest | S | 3 | After settle() restores controls.enabled and damping may keep running; confirm needsRender returns to false post-settle and only re-arms on a real change so a resting wheel draws zero composited frames. | The resting state is where most sessions sit between spins, so stopping the expensive composited frame there matters. |
| Suspend flame loop while spinning | S | 3 | useFlame's loop() runs its per-frame radial-gradient fills continuously regardless of wheel state; expose a pause flag set true during a spin (App toggles ui.spinning) so the ember canvas stops redrawing. | The flame's per-frame fills are pure overhead during the one moment the GL wheel must hit 60fps. |
| Vibration capability probe and graceful tier | S | 3 | Cache a one-time capability check (navigator.vibrate present, gamepad connected) so haptic calls no-op cleanly on desktops without a pad and the playSpin hot path never pays a failed-call cost per frame. | It keeps the per-frame spin loop clean and is the foundation every other haptic idea relies on. |
| Free segment geometry cache on shrink | M | 3 | getSegmentGeometry stores ExtrudeGeometry per rounded angle in segGeoCache, only disposed in dispose(), so after a build() with fewer segments evict cached angle keys no longer in use. | Weight edits produce many distinct angle keys, and without eviction the cache accumulates heavy beveled geometries across a session. |
| Merge divider lines into one geometry | M | 3 | build() adds one THREE.Line per segment reusing sharedDividerGeo rotated by startAngle, so build a single LineSegments BufferGeometry with all N rotated divider endpoints added once. | Eliminates N line objects and their per-frame matrix updates, which all rotate rigidly together anyway. |

### Backend (66)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Snapshot pre-spin state, not post-spin | S | 5 | memory.rs spin() builds _before_snapshot then discards it and create_snapshot stores parts.clone() after the pick, so undoing a spin restores the already-removed roster. | Fixes a real correctness bug where undoing a spin in useHistory returns the participant still removed. |
| Causal snapshot lookup by log index | M | 5 | memory.rs:242 selects snapshots via lexical `s.action_id.as_str() <= before` on random UUIDs, which is not chronological; index snapshots by their position in the session action vector and add an api.rs test that undo-to-action returns the correct prior roster. | Undo and history preview project from this broken comparison and currently restore the wrong snapshot. |
| Fix snapshot ordering so scrub lands right | M | 5 | memory get_snapshot compares action_id UUIDs with string <= (the comment admits it) and sqlite ignores before_action_id entirely; resolve 'before' via a real sequence, not UUID lexical order. | HistoryTimeline scrub and restoreTo currently land on the wrong state because the before filter is effectively ignored. |
| Spin seed broadcast for shared deceleration | M | 5 | handlers::spin and SessionEvent::SpinResult send only picked and remaining, so each remote client's playSpin picks its own random offset and rotation count; add a u64 seed and target angle so every client reconstructs the identical cubic-ease path instead of computing a different spin. | Remote viewers should see the same suspenseful spin the room sees, not a divergent one. |
| Atomic spin action and snapshot write | M | 5 | In memory.rs and sqlite.rs, spin() commits the participant update, then append_action and create_snapshot run afterward with let _ = so their errors are ignored. | A crash between the commit and the append leaves a spun-but-unlogged state the history can never reconstruct. |
| Deterministic spin seed in action payload | M | 5 | ActionKind::Spin in models.rs records only picked_id; memory and sqlite spin() call rand::rng() with no captured seed. Add a seed plus the active-id set to the Spin payload. | Captured seeds make spins replayable from the event log so an audit can reconstruct the exact pick without trusting picked_id. |
| Monotonic per-session action sequence | M | 5 | list_actions in sqlite.rs orders by timestamp DESC which collides at ms resolution; add a per-session seq:u64 column incremented under begin_write so snapshot lookup and history scrub get a real total order. | Lexical UUID and ms-timestamp ordering is unreliable for the history scrub and restore lookups that depend on it. |
| Room passcode gate on session create | L | 5 | CreateSessionRequest in models.rs takes only a title; add an optional passcode the store hashes (argon2) and require an X-Room-Pass header verified in ensure_session_exists before any mutating handler runs. | Today the UUID in the URL is the only secret, so a leaked link gives full spin/delete/reset control with no second factor. |
| Capability tokens split host from viewer | L | 5 | create_session in handlers.rs returns the raw session id used for every action; mint a host token (spin, delete, reset, restore) and a read-only viewer token, and check scope in those handlers. | A single all-powerful link lets anyone you share the wheel with secretly spin or wipe the roster. |
| Lock CORS to known origins | S | 4 | main.rs builds CorsLayer with allow_origin(Any); replace it with an ALLOWED_ORIGINS env allowlist so arbitrary sites cannot drive the mutating REST endpoints. | Wide-open CORS lets any page issue session-id-only spins and deletes on behalf of a victim who shared a link. |
| Resume AudioContext on first gesture | S | 4 | initAudio constructs AudioContext in setup but browsers keep it suspended until a user gesture; add a one-shot pointerdown/keydown listener that calls audioCtx.resume() and clears itself. | Without this the very first spin's ticks are silently dropped on most browsers. |
| Typed action payloads end to end | S | 4 | sqlite list_actions reparses kind via a string match with a _ => Reset fallback, so an unknown or future action kind silently becomes Reset and corrupts any replay over it. | A malformed action kind is currently swallowed as a Reset, breaking event replay. |
| Validate restored snapshot belongs to session | S | 4 | restore_from_snapshot accepts an arbitrary participants list from RestoreRequest and blindly replaces the roster with no check that ids match the session. | A client can post foreign or fabricated participants and overwrite the roster wholesale. |
| Check WebSocket origin on upgrade | S | 4 | session_ws in handlers.rs upgrades any caller that names an existing session; read the Origin header before ws.on_upgrade and reject sockets from origins outside the configured allowlist. | Cross-site WebSocket hijacking lets a third-party page silently subscribe to a room's live name and spin stream. |
| Reset emits a logged Reset action | S | 4 | reset_session in handlers.rs clears removed flags but never appends ActionKind::Reset; the enum variant exists (models.rs:97) and is already serialized in the stores yet is never written. | Reset is invisible in history and replay, so undo cannot step across a reset boundary. |
| Broadcast reorder so order stays shared | M | 4 | roster.ts reorder mutates participants locally and never calls the API, and ws.rs has no reorder event, so add a reorder endpoint that logs the already-defined ActionKind::Reorder and broadcasts it so NameList order converges. | Reorder is currently a silent local-only edit that desyncs every other client's list. |
| Capture spin weights in the log | M | 4 | spin() in memory and sqlite stores ActionKind::Spin{picked_id} only, never the WeightedIndex weights it sampled, so later weight edits silently change replay outcomes; snapshot the weights into the Spin action. | Replaying a Spin after a weight change picks differently than the recorded reality. |
| Persist explicit ordering column | M | 4 | The participants table has no position column and list_participants returns DB row order, so add a position integer set on add and updated by Reorder. | NameList drag order and reorder events have nowhere to durably land, so order is non-deterministic across backends. |
| Thread a seed through the server spin | M | 4 | MemoryStore::spin and the sqlite spin sample WeightedIndex with rand::rng(), so accept an optional seed param and use StdRng::seed_from_u64 to make the picked id reproducible. | A seeded server spin lets the api.rs spin tests assert an exact winner instead of just set membership. |
| Unify eventLog and history action log | M | 4 | roster.ts recordEvent() pushes to a separate eventLog with a comment that it would go to useHistory.recordAction, so route recordEvent through the injected useHistory so one timeline reflects all mutations. | Two divergent logs mean HistoryTimeline silently misses roster edits, undermining the single source of truth. |
| AI bias nudge from real spin counts | M | 4 | Read the action log via api.listActions plus eventLog Spin entries, then suggest weight nudges (e.g. 'Dave picked 0 of 8, bump to 2x?') as an accept-on-click chip above the InspectorPanel 0.1-10 slider. | Replaces AnalyticsPanel's static mock bias note with a concrete fairness fix grounded in real counts. |
| Broadcast spin_started before spin_result | M | 4 | handlers::spin computes and broadcasts SpinResult in one shot; emit a SpinStarted event first (seed plus duration) so remote clients begin playSpin immediately and SpinResult only confirms the landing. | Cuts perceived latency so the wheel starts turning for everyone at once instead of after the RNG round-trips. |
| Spin lock to a single host | M | 4 | spin in handlers.rs accepts a POST from anyone with the id and broadcasts SpinResult to all; gate it behind a host-held token so spectators cannot trigger eliminations. | In a live shame ceremony, anyone with the link can currently fire a spin and remove a real person. |
| Record spin seed for deterministic replay | M | 4 | ActionKind::Spin stores only picked_id; capture the RNG seed used in memory.rs spin() so a replayed Spin re-derives the same pick instead of trusting a stored id. | Without the seed a spin is a fact, not a reproducible event, which blocks true event-sourced replay. |
| Tombstone removed participants | M | 4 | delete_participant hard-removes the row in both stores while Spin sets removed=true; converge on a removed flag so deleted ids referenced by earlier Add/Spin actions do not vanish from replay. | A hard delete erases an id that earlier actions reference, breaking the event fold. |
| Wire Reorder through the store layer | M | 4 | ActionKind::Reorder {from,to} exists in models.rs and sqlite serialization but has no Store method, route, or handler, so roster.ts reorder mutates locally only. | Reorder is logged-shaped but never persisted, so arrangement is lost on reload and never reaches other clients. |
| Detect duplicate-person spellings | M | 4 | Run participants through an AI similarity pass (Sasha vs Alexander vs Александр) keyed off hashName collisions in identity.ts, surfacing a merge suggestion in NameList.vue near the paste-confirm strip. | parsePastedNames only dedupes exact case-folded matches, so the same person appearing twice skews odds invisibly. |
| App-level ping/pong heartbeat with RTT | M | 4 | useWebSocket detects death only via ws.onclose; add a periodic client ping (handled in handlers.rs's currently empty _ => {} client arm) and surface round-trip time next to wsConnected. | Catches half-open sockets that never fire onclose and gives a real latency signal for other affordances. |
| Optimistic add reconciled by client op id | M | 4 | roster.addName reconciles temp- ids only by matching the returned server id, so a remote participant_added can land before the POST resolves and create a duplicate; carry a client_op_id on AddParticipantRequest echoed in ParticipantAdded to dedupe deterministically. | Eliminates the visible duplicate-then-vanish flicker when your own add echoes back over WS mid-flight. |
| Enable preserveDrawingBuffer for PNG capture | M | 4 | ThreeWheelRenderer creates WebGLRenderer with antialias and alpha but no preserveDrawingBuffer and captureCanvas returns the live domElement, so toDataURL reads back blank between frames; add a one-shot capture path that forces a render then reads pixels. | Every PNG or poster export silently depends on this or produces a transparent frame. |
| Logged inverse action instead of snapshot undo | M | 4 | useHistory.undo fetches a snapshot and setParticipants, while a comment notes a Restore event is the real model; emit an inverse action appended to the log instead. | Snapshot-swap undo is invisible to other clients and to replay, whereas a logged inverse keeps the event stream authoritative. |
| Validate snapshot restore payload server-side | M | 4 | restore_from_snapshot takes a client-supplied RestoreRequest{participants} and writes it wholesale; verify every participant.session_id matches the path id and that ids existed before, so a caller cannot inject arbitrary names or foreign ids. | The endpoint blindly trusts a full participant array, letting any caller overwrite the roster with fabricated entries. |
| Idempotent snapshot restore by id | M | 4 | Both stores implement restore as DELETE-then-reINSERT of the whole participant list keyed on a client array, so concurrent adds are lost and re-running duplicates work; key restore on a snapshot id and make it a safe repeatable operation. | Undo should be repeatable and concurrency-safe, not a destructive full-table swap. |
| Per-session spin and write rate limit | M | 4 | spin, add_participant, add_participants_batch and reset_session in handlers.rs have no throttle; add a per-session token bucket in AppState keyed by session_id returning 429 when a client floods spins or adds. | An unauthenticated caller can hammer spin or batch-add to exhaust the roster or DoS the in-memory store. |
| Single source of truth for action log | M | 4 | roster.ts keeps eventLog with client-minted evt- ids while useHistory.actions holds server actions, and the two logs never reconcile, so history UI and replay can disagree about what happened. | Two divergent logs are a correctness hazard for any time-travel or replay feature. |
| Strip session id from URL after load | M | 4 | create() in useSession.ts writes window.location.hash to #/<id>, exposing the all-powerful id in history and screen shares; after load, swap the hash for a local handle and keep the real id in memory. | The hash is the de facto password, sitting in shoulder-surfable URL bars and shared-screen recordings. |
| Name moderation filter before broadcast | M | 4 | add_participant and add_participants_batch in handlers.rs only trim and length-check names, then broadcast them verbatim; add a configurable blocklist that rejects or masks entries before they reach the store and wheel. | Names are shown to the whole room, so an unmoderated slur or doxxing string is published to every connected client. |
| Undo grouping for reset action | M | 4 | reset_session in handlers.rs clears removed and spin_order on every participant but logs no Reset action and creates no snapshot, even though ActionKind::Reset exists, so a reset is irreversible in history. | Reset is the most destructive action yet it is invisible to undo, the one place users most expect to recover. |
| Idempotent spin via client-supplied spin id | M | 4 | api.spin() and the /spin handler take only a session id; accept an optional spin_id and have the store return the original SpinResult for a duplicate id instead of spinning again. | A retried POST after a flaky reconnect currently eliminates a second person silently. |
| Monotonic event sequence on every broadcast | M | 4 | SessionEvent variants in ws.rs carry no ordering; add a per-session seq counter in Hub::broadcast and stamp every event so useSession.onMessage can detect gaps and the Lagged branch becomes recoverable. | It turns the lossy 64-slot broadcast into a gap-detectable stream, the prerequisite for real catch-up. |
| Backfill weight and visual in YDB | M | 4 | ydb-schema.sql omits weight and visual columns, and participant_from_row in ydb.rs hardcodes weight:None, visual:None, dropping them on read. | Switching STORE_BACKEND to ydb silently discards weighting and segment styling that the memory and sqlite backends persist. |
| Stable ids in event replay fold | M | 4 | replayEvents in roster.ts mints replay- ids for Add events, so later Remove and Spin payloads that reference the real ids never match during the fold. | Pure event sourcing is broken for any session past add-only, because removes silently no-op. |
| Undo reverses weight and visual edits | M | 4 | useHistory undo() only calls setParticipants from a snapshot; UpdateWeight and UpdateVisual actions (handlers.rs:275,286) have no snapshot so they cannot be undone, leaving editor edits stranded. | Undo silently ignores the editor's own weight and color edits, surprising given they are logged actions. |
| Snapshot version guard on restore | M | 4 | restore_from_snapshot overwrites the slot with posted participants; if another client spun between snapshot fetch and restore the spin is lost, so carry the base action id and reject the restore if the head moved, surfacing a merge prompt. | A history restore stops silently erasing spins that happened concurrently. |
| Incoming webhook on every spin | M | 4 | In handlers::spin after hub.broadcast(SpinResult), POST the same picked and remaining JSON to per-session webhook URLs stored alongside Session, reusing the SessionEvent serde shape as the payload. | One generic webhook unlocks Zapier, Make and n8n flows without per-tool code. |
| Server-rendered OG card per session | L | 4 | The axum router serves only JSON; add GET /og/{id}.png that reads the snapshot projection and rasterizes the recap podium so shared links unfurl with a real preview. | A bare hash link unfurls as nothing in chat apps, killing reshare appeal. |
| Resync request when client detects seq gap | L | 4 | handle_ws ignores all client messages; accept a {resync_from:seq} text frame and replay missing SessionEvents from the action log so a Lagged or briefly-dropped client recovers without the full REST getSession refetch in attemptReconnect. | Avoids a full roster reload for a one-event hiccup while keeping optimistic state intact. |
| Rotate room id on suspected leak | L | 4 | Session.id in models.rs is fixed at creation and doubles as the access secret; add a host-only rotate endpoint that issues a new id, invalidates the old WS channel in the Hub, and forces reconnect. | Once a link leaks there is no way to cut off an unwanted viewer short of abandoning the room. |
| Real presence roster over the WS uplink | L | 4 | ui.ts presence is a dead mock Record<string,string>; on session_ws upgrade register the connection in Hub and broadcast PresenceJoined/Left so presence reflects real connected peers with identityColor dots. | Replaces fake presence with a true who-is-here list, the foundation for every other collaboration cue. |
| Service worker for offline shell | L | 4 | main.ts mounts a Vite SPA with a preloaded Roboto-Bold.ttf and a lazy three.js chunk; precache the app shell, font, and wheel chunk so a parked session reopens instantly without network. | Sessions are often run live in a room with flaky wifi, and a cached shell removes the cold-load gap before the wheel paints. |
| WS catch-up cursor replaces full REST refetch | L | 4 | attemptReconnect in useSession.ts calls api.getSession then setParticipants, discarding optimistic local edits; instead pass the last applied seq on the WS query string and replay only events after it from the actions store. | Reconnect stops clobbering in-flight local adds and is far cheaper on large rosters. |
| Branch on restore instead of overwrite | L | 4 | restore_from_snapshot in handlers.rs appends one SnapshotRestored action and overwrites participants; instead fork a named branch so the abandoned future is preserved and switchable. | Restoring silently discards everything after the chosen point, the classic undo-then-redo data-loss trap. |
| Command aliases boost fuzzy recall | S | 3 | CommandPalette fuzzyScore searches only [label, hint, subtitle], so add an optional keywords array to Command (e.g. remove, kick for Eliminate) and include it in those fields. | Users type kick or delete but the only matchable text is Eliminate. |
| Reject control chars and bidi in names | S | 3 | Name validation in handlers.rs only trims and counts chars, so also strip or reject control characters, zero-width spaces, and bidi-override codepoints before Participant::new. | Bidi and zero-width tricks let an attacker disguise one name as another or inject invisible payloads into every client. |
| Polling actions endpoint with cursor | S | 3 | Add GET /api/v1/sessions/{id}/actions?since={action_id} returning Action entries after a cursor, so polling integrations dedupe on Action.id without a webhook. | Many no-code tools only poll; a cursor endpoint makes the action log consumable by them. |
| Stamp action source in the log | S | 3 | Thread an actor/source string from API and bot callers into Action.actor (currently always None) so list_actions distinguishes UI spins from integration-triggered ones. | Makes the action log auditable by origin, which integrations need for trust and debugging. |
| Webhook on survivor declared | S | 3 | When SpinResult.remaining hits 1, emit a distinct survivor event computed from activeParticipants, separate from per-spin events, mirroring buildRecapSummary's survivor logic. | Lets integrations react only to the run's conclusion, not every intermediate spin. |
| Debounce rapid spin requests server-side | S | 3 | doSpin in useSession.ts and spin in handlers.rs allow back-to-back POSTs with no minimum interval; enforce a per-session cooldown in the store so picks cannot be rushed faster than the animation or spammed to drain the roster. | Rapid-fire spins both desync clients mid-animation and let a griefer eliminate everyone instantly. |
| Server-side ownership check on delete | S | 3 | delete_participant in handlers.rs broadcasts ParticipantDeleted unconditionally; the handler should surface NotFound and skip the broadcast so a forged pid for another session cannot emit phantom delete events. | Broadcasting a delete for an id the store rejected can desync every client's roster and leaks that an id exists elsewhere. |
| Bound total roster size per session | S | 3 | add_participants_batch caps at MAX_BATCH_NAMES=500 per call but nothing limits total roster size or call frequency; add a per-session participant ceiling enforced in the store so repeated batches cannot balloon memory. | 500 per call with unlimited calls is an easy memory-exhaustion and griefing vector on the in-memory store. |
| Anomaly note in analytics | S | 3 | AnalyticsPanel shows top-removed counts as bars; have an AI scan the stats computed and write a one-line note when a name's frequency departs sharply from uniform. | The bars show counts but not whether they are surprising; the note adds the interpretation that makes the data actionable. |
| Same 404 for missing and forbidden rooms | S | 3 | ensure_session_exists returns NotFound for a missing session; once passcodes exist, a wrong passcode should return the same NotFound shape so an attacker cannot distinguish a real-but-locked room from a nonexistent id. | Distinguishable responses let an attacker enumerate which session ids are valid before brute-forcing access. |
| Share spin_order logic across backends | S | 3 | spin_order in spin() is computed as removed count +1 independently in memory, sqlite, and ydb with no shared helper, so the three can diverge. | Three copies of the ordinal logic risk one backend numbering removals differently than the others. |
| Validate visual color and icon fields | S | 3 | update_participant_props in handlers.rs clamps weight to 0.1-10 but passes SegmentVisual.color_override and icon through unchecked; validate color against a CSS-color regex and icon against an allowlist before storing and broadcasting. | Unvalidated visual fields are broadcast to every client and rendered, an injection and resource-abuse vector via crafted strings. |
| Caps-aware shortcut normalization | S | 3 | useHotkeys compares e.key.toLowerCase() for some keys but raw e.key for others (?, /, n), so CapsLock or a held Shift can break bindings; route all single-key checks through one matcher that accounts for shifted symbols. | Inconsistent case handling makes shortcuts silently fail under CapsLock, the kind of bug that erodes trust in the grammar. |
| Coalesce rapid weight edits into one | M | 3 | handlers.rs update_participant_props appends a separate UpdateWeight action per call, so dragging the InspectorPanel range floods the log; debounce-merge consecutive same-id weight actions server-side. | One slider drag can produce dozens of log entries, burying meaningful events. |

### DevEx (28)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Set __spinStarted so verify probe stops guessing | S | 4 | bench/verify.mjs grids 0.25-0.75 by 0.7-0.95 clicking the canvas and gates on window.__spinStarted, which playSpin never sets anywhere in src, so set it true at playSpin start and false on settle. | The e2e spin probe currently relies on a fragile body-text heuristic that can never confirm the spin actually launched. |
| Resolve picked_id to name in history | S | 4 | formatAction in HistoryTimeline.vue:55 prints payload.picked_id.slice(0,8), a raw UUID stub; look the id up in roster.participants and show the eliminated name. | A truncated UUID is unreadable, and the whole point of a shame log is seeing who got eliminated. |
| Test prevent-repeat plus bias interaction | S | 4 | WheelEngine.getEffectiveSegments applies filterPreventRepeat then multiplies biasMap and pick() depends on both; add tests asserting the last-picked id is excluded and a 3x bias roughly triples relative odds together. | These two rules compose in one private method, exactly where a subtle elimination bug would live. |
| Use WheelEngine's injectable rng end to end | S | 4 | weightedPickIndex and simulateSpins already accept rng but WheelEngine.pick() and simulate() call them with default Math.random; add a constructor rng so engine picks are deterministic. | The pure engine is the natural seam for property tests but is currently non-deterministic, blocking exact-pick assertions. |
| Wire up the dead __wheelDebug renderer hook | S | 4 | inspect.mjs reads window.__wheelDebug scene, wheelGroup and renderer but ThreeWheelRenderer never sets it so snap() always returns null; expose it from the constructor behind an import.meta.env.DEV guard. | The scene and geometry-count inspector is already written but silently no-ops, so geometry leaks go uncaught. |
| Add CI workflow for vitest and cargo test | S | 4 | There is no .github/workflows directory; the frontend has vitest and the backend has tests/api.rs. Add one workflow running vitest, cargo test, and vue-tsc on PRs. | Nothing enforces the existing test suites on push, so a regression can land green. |
| Single brand-accent token replaces hardcodes | M | 4 | #4ECDC4 is repeated across NameList, ToastStack, AnalyticsPanel, HistoryTimeline, CommandPalette, SpinResult, and App.vue while style.css defines --accent only for focus rings; promote --accent to the one brand source and rewrite every literal to var(--accent). | A retheme today means hunting many files; one token makes the glass UI reskinnable in a single edit. |
| Glass surface token scale | M | 4 | Glass values drift per component (panel rgba(60,60,60,.75)/blur16, menus rgba(30,30,30,.92)/blur12, dock .78/blur8, toasts .82/blur8); define --glass-1/2/3 fill+blur+border and have .panel, .recents-menu, .action-dock, and ToastStack consume them. | Unifies the frosted look so every floating surface reads as one material instead of four near-misses. |
| Assert renderer disposes all geometries | M | 4 | build() clears the group and dispose() frees segGeoCache via userData.sharedGeometry flags; add a test driving build->rebuild->dispose that asserts renderer.info.memory.geometries returns to baseline. | The shrink path historically rebuilt geometry per frame, so a leak regresses silently without a memory assertion. |
| Seeded scenario URL for repro sharing | M | 4 | Once playSpin and the store accept a seed, encode roster+seed into a query param the app reads on load so a teammate opening the link sees the identical wheel and spin outcome. | Turns any visual bug report into a one-click deterministic repro instead of a vague description. |
| Debug overlay for live spin telemetry | M | 4 | playSpin computes per-frame eased t, currentRotation and the SLOWMO_START branch every frame; render a DEV-only fixed corner panel showing t, deg/s, slowmo flag and last tick id. | Spin-feel tuning (slowmo, hitstop, squash) is done blind today; a numeric readout makes the easing legible while iterating. |
| Replay-events fold test over recorded log | M | 4 | roster.replayEvents folds Add/Remove/UpdateWeight/Spin events into participants then syncEngine; add a test feeding a canned event sequence and asserting the derived active and removed sets. | The store advertises event sourcing as its foundation but the fold reducer has no coverage of its branch matrix. |
| Prove landing maps back to the winner | M | 4 | playSpin computes target = PI/2 - segPoint then feeds currentRotation into segmentIdAtRotation for ticks; add a test that the final target rotation maps back to the intended winner index for N in 2..50. | The landing-angle math and the tick-mapping math are tested separately but never proven mutually consistent, exactly where an off-by-slice would hide. |
| Backend property test for spin elimination | M | 4 | MemoryStore::spin marks one active participant removed and increments spin_order, but api.rs tests only check 2-3 names; add a proptest looping N spins asserting exactly N removed, monotonic spin_order, and no double-pick. | The elimination invariant is the product's core promise and is only spot-checked today. |
| ShortcutsSheet keymap from one source | M | 4 | ShortcutsSheet.vue hardcodes a shortcuts array that a comment says must mirror onGlobalKeydown by hand; export one keymap descriptor (key, label, predicate) from useHotkeys and render both the handler dispatch and the sheet from it. | A hand-maintained cheat sheet silently rots, so one source of truth keeps the self-teaching surface honest. |
| Property test weighted pick distribution | M | 4 | weightedPickIndex in utils/wheel takes an injectable rng; add a vitest that drives a seeded generator over many picks and asserts each id's frequency tracks its weight share within a tolerance band. | The weighting math is the core fairness contract and has no statistical coverage today. |
| Inject seeded RNG into playSpin offsets | M | 4 | playSpin computes randomOffset and the number of full turns via Math.random(), both unseedable; accept an optional rng on the renderer for deterministic landing geometry. | Without a seedable spin the visual landing angle can never be snapshotted or replayed in a test. |
| Snapshot-testable 2D default renderer | L | 4 | The WheelRenderer interface exists but only ThreeWheelRenderer implements it, and nothing renders headlessly; stub a 2D canvas/SVG renderer behind the same interface and snapshot its output for a fixture roster as the CI default. | A deterministic 2D renderer is the keystone for headless testability and reduced-motion fallback. |
| Persist last camera pose | S | 3 | On controls change, debounce-save azimuth/polar/distance to localStorage like the muteKey pattern and restore into camera and controls before fitWheel in initScene. | Presenters who find a good projector angle lose it on reload; persisting the pose respects that setup. |
| Export preset remembers last via frecency | S | 3 | CommandPalette already implements frecency ranking; have the SVG/WebM/PDF dock buttons record usage through it so the most-used export floats to a single default action. | Hosts repeat one export format; surfacing it cuts the dock to one tap. |
| Public streaming WebSocket event contract | S | 3 | Document and version the existing /api/v1/sessions/{id}/ws SessionEvent JSON and serde tag names in ws.rs so external clients can rely on the real-time feed. | The broadcast hub is already a real-time feed, and naming it a contract makes it integrable. |
| Injectable clock and id for reproducible replay | S | 3 | recordEvent in roster.ts builds ids from Date.now()+Math.random; add an injectable now() and id() so event-log ordering is reproducible in tests (useToasts.test already fakes timers). | Event-log replay tests cannot assert ordering while ids embed wall-clock time and entropy. |
| Expose real frame deltas from the render loop | S | 3 | verify.mjs injects its own rAF frame-delta collector, but startRenderLoop already has the canonical frame timing; push deltas into a capped window.__frames ring in DEV. | Replaces brittle injected timing with the renderer's real loop deltas, giving honest p95/max during the shrink. |
| Promote bench scripts to npm test:e2e | S | 3 | bench/verify.mjs, inspect.mjs and verify-reconnect.mjs run only via raw node with a hardcoded Chrome Dev path; add package scripts and an env override for the executable. | Three real verification harnesses exist but are invisible from package.json, so they rot unrun. |
| Deterministic fixture roster builder | S | 3 | verify.mjs, inspect.mjs, profile.mjs and wheel-bench.mjs each hardcode the same twelve Cyrillic names; extract a shared fixtures module (names, weights, seed) imported by the benches and vitest. | Four copies of the same roster drift independently and make cross-tool comparisons unreliable. |
| Golden SVG snapshot of exportWheelSVG | S | 3 | useExports.exportWheel calls exportWheelSVG(active, title), which duplicates the renderer's computeAngles math; snapshot the SVG string for a seeded fixture roster in vitest. | The SVG export duplicates angle math and has no regression net, so a golden file catches divergence from the wheel. |
| Fuzz computeAngles sum invariant | S | 3 | computeAngles clamps with Math.max(0.1, weight) over a total; add a property test over random weight arrays (including zeros, NaN, negatives) asserting angles always sum to 2PI and stay non-negative. | Edge weights reach this function from preview drags and the clamp is untested under adversarial input. |
| Public OpenAPI spec for the API | M | 3 | Generate an OpenAPI document from the routes.rs route table and models.rs serde types (Session, Participant, SpinResult, Action) so integrators get typed clients. | A real spec is the difference between a hobby endpoint and an integrable public API. |

### Ritual (12)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Reinstate a name back into the wheel | S | 4 | A per-row action on removedParticipants that clears removed, removed_at and spin_order for one id (the fields resetRoster touches), re-runs syncEngine and records a Reinstate event, so a wrongly-eliminated person rejoins without a full reset. | Facilitators sometimes need to undo a single elimination without rewinding the whole run. |
| Last-three-ticks ritardando emphasis | M | 4 | The frame loop knows spinSegmentIds length and current index, so the remaining-segment count to the winner is computable; detune and lengthen the final 3 ticks as a landing tell. | A deliberate slowing on the last ticks is the classic game-show landing cue. |
| Safe-list a name out of this round | M | 4 | A per-row 'Safe this round' toggle sets a transient skipUntilRound flag; syncEngine filters those ids from engine.setSegments like preventRepeat does, and the segment renders ghosted via updateInPlace until the round resets. | Facilitators routinely exempt someone for one round without deleting them from the roster. |
| Underdog catch-up per round | M | 4 | A toggle that, on each RoundComplete, calls engine.suggestLeastRecentBias and applies the returned weight via roster.updateWeight so least-picked names fatten, with the odds donut visibly growing. | Keeps long elimination games from feeling rigged toward the same people by automating the spread-the-pain bias. |
| Consent gate before eliminating a name | M | 4 | An opt-in consent mode that, on playSpin landing, holds the SpinResult lower-third with Accept/Veto; Veto records a Reroll in eventLog, restores the picked id (uncommitted until dismissWinner calls applySpinResult), and re-spins. | Some facilitation contexts need the room or picked person to consent before a name locks, avoiding an irreversible awkward result. |
| First-run keyboard tour overlay | M | 4 | On first session (no wheel-editor flag, empty frecency) run a 4-step coachmark in App.vue pointing at Spin (Space), the add field (/), Tab roving, and Cmd-K, each advanced by pressing the actual key. | Teaching keys by making the user press them once converts the shortcut from trivia into muscle memory. |
| Knockout rounds that re-seed survivors | L | 4 | Add a Rounds mode where each spin removes the picked id and, when activeParticipants hits a host-set survivor count, the round freezes and a Start Round 2 control re-arms instead of auto-opening recap. | It turns the wheel into a real multi-round tournament instead of spinning until one person is left. |
| Abstain pile separate from picked | S | 3 | Add a third NameList bucket for participants the facilitator marks as sitting out, filtered from syncEngine candidates like removed ones but rendered in a distinct neutral section with no medal and no spin_order. | Conflating eliminated and opted-out muddies the shame ritual, so separating them keeps the podium honest. |
| Persist last session for quick resume | S | 3 | App.vue reads the #/id hash on mount and otherwise shows the create screen; store the last opened session id and, in standalone mode, deep-link straight into it. | An installed app icon should resume the running session, not dump the user on the empty create screen. |
| Locked roster freeze during a round | S | 3 | A round-lock flag that disables roster.add/remove/reorder and NameList's add field while spinning is true or a round is in progress, with a lock glyph in the phase strip, unlocking at RoundComplete. | Stops mid-ritual roster tampering (adding yourself out, removing a rival) that would undermine an elimination's fairness. |
| Ceremonial countdown before first spin | S | 3 | An optional 3-2-1 overlay before the first playSpin of a session, each number firing playTick at rising progress and the wheel doing its existing 0.97 anticipation squash on '1' before launch. | A shared countdown gets a distributed room to spin together and reuses sound and squash hooks that already exist. |
| Nomination round before the spin | M | 3 | Add a Nominate mode where peers double-click rows to add +1 weight via updateWeight, accumulating a visible nomination badge on NameList rows, then the host spins the nomination-biased wheel. | Encodes the common who-should-we-pick-on step directly into segment weights instead of doing it verbally. |

### Feature (72)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| One demo spin from empty card | M | 5 | Add a Show me a spin button to NameList's empty-card that loads the existing SAMPLE_NAMES via the add-batch emit, runs one handleSpin, then offers to keep or clear the roster. | Letting first-timers feel a real elimination before committing names is the fastest path to the aha moment. |
| Curve each name along its segment arc | L | 5 | Replace makeSimpleLabel's flat PlaneGeometry(1.6,0.32) with opentype.js glyph outlines (the loaded _otFont, currently only void-touched) laid along the WHEEL_RADIUS arc so each name follows its slice. | Arc-fitted text is the signature look of a real prize wheel and uses the font already loaded but wasted. |
| Wire dead theme-accent var into chrome | S | 4 | ui.ts updateThemeColor writes --theme-accent on documentElement and App renders the color picker, but no CSS rule ever reads --theme-accent, so bind --accent to fall back through --theme-accent. | Ships a feature that has store, persistence hook, and UI but currently does nothing when used. |
| data-theme attribute over class swap | S | 4 | toggleTheme sets document.documentElement.className, clobbering other root classes and supporting only default and high-contrast; switch to dataset.theme so skins coexist with high-contrast and reduced-motion states. | The current className assignment caps the theme system at exactly two mutually exclusive states. |
| Exact live odds bars in panel | S | 4 | AnalyticsPanel only shows removed counts and the mock biasEffect string; feed it computeAngles(activeParticipants) so each active name gets a real percent-to-be-picked bar instead of the fake bias note. | Turns the placeholder bias line into the one number players actually care about before a spin. |
| Native share sheet for links | S | 4 | copyLink in App.vue only calls navigator.clipboard.writeText with the hash URL; branch to navigator.share({url,title}) when available so the Share button opens the OS sheet. | On mobile copy-to-clipboard is a dead end, while the share sheet hands the session into the group's messaging app. |
| Full session JSON archive export | S | 4 | listActions and getSnapshot expose the entire append-only log and projected roster; add exportArchive() in useExports that bundles session, actions, and final snapshot into a downloadable .json. | The event-sourced backend already holds everything, so one download makes a session portable for backup or replay. |
| Welcome-back resume banner | S | 4 | On load, if useSession's recentSessions has a most-recent entry and the URL hash is empty, show a Resume <title> banner on the create screen wired to the existing load(id) path. | Returning users almost always want their last wheel, not a blank create form. |
| Renderer reads body background token | S | 4 | body background is #1e1e1e in style.css and the renderer separately calls setClearColor(0x1e1e1e); have ThreeWheelRenderer read computed --bg from the canvas host so the clear color tracks any theme backdrop. | A light or sepia skin would otherwise leave the canvas a mismatched dark void against re-themed chrome. |
| Add PWA manifest and icons | M | 4 | index.html has only favicon.svg and a viewport meta and no manifest link, so add manifest.webmanifest with standalone display, theme_color #1e1e1e matching the renderer clear color, and maskable icons in public/. | Without a manifest the app cannot be installed or run full-screen, the baseline for every other mobile capability. |
| Fairness meter from running pick counts | M | 4 | AnalyticsPanel already tallies nameCounts over props.removed, so add a single Gini-style fairness bar (0 = perfectly even, 1 = one name dominates) computed from those counts versus uniform expectation. | Gives hosts one honest number for whether the wheel has treated the group evenly across the session. |
| Feed theme accent into wheel bloom | M | 4 | ThreeWheelRenderer hardcodes UnrealBloomPass(0.6,0.5,0.85) and clearColor 0x1e1e1e independent of the UI theme; add setThemeAccent(hex) tinting bloom and key light so the 3D wheel and glass chrome share one palette. | The canvas and the DOM theme are two disconnected color worlds today; this stitches them. |
| Inline weight entry per participant | M | 4 | Give each participantCommand an inline config whose submit parses a number and calls roster.updateWeight(p.id, n), reusing the existing CommandInline path so @Bob then a value sets weight without the slider. | Reuses the inline machinery to make weight edits a two-keystroke flow for power users. |
| Monte Carlo survival odds per name | M | 4 | Run rosterStore.simulateHeavy(2000) over activeParticipants in the worker, tally how often each id survives to the end, and render a survives-probability column in AnalyticsPanel. | With weights and prevent-repeat, true survival odds diverge from per-spin odds and only simulation reveals it. |
| One-tap winner PNG from canvas | M | 4 | captureCanvas returns the three.js domElement but useExports only wires WebM and SVG; add exportFrame() that renders once with the winner highlighted via setHighlight and downloads a PNG poster. | Hosts want the single triumphant frame more often than a full clip. |
| Template gallery as first-run picker | M | 4 | On the create screen surface the SEED_TEMPLATES as clickable cards (reuse TemplateGallery markup) instead of the bare select dropdown, each showing its name count and weight badges. | Visible template cards beat a hidden dropdown for getting a populated wheel in one click. |
| Best-of-N series scoreboard above wheel | M | 4 | A facilitator sets a series length; each playSpin onComplete tallies a win in a new uiStore.seriesScore map keyed by participant id, rendered as a compact tally row next to the analytics span in App.vue's header. | Lets recurring rituals resolve a multi-round series rather than a single spin, matching how facilitators run elimination. |
| Sudden-death tiebreak between two survivors | M | 4 | When activeParticipants.length===2 a 'Sudden Death' affordance forces preventRepeat off via togglePreventRepeat, equalizes both to 1x through engine.equalize, and tags the next playSpin so SpinResult shows a 'Sudden Death' eyebrow. | Gives the final two a distinct dramatic ceremony rather than an ordinary spin, matching how facilitators stage a decider. |
| Turn-order queue from spin history | M | 4 | A 'Speaking order' panel reads removedParticipants (already sorted by spin_order) as an ordered checkable agenda; checking a name advances a highlighted cursor and broadcasts via the existing WS result channel. | The elimination order is exactly the turn order for standups and retros, so surfacing it as a runnable agenda is the actual deliverable. |
| Purge per-session localStorage on leave | M | 4 | useComments (wheel-comments-<id>), useSession recents (wheel-recent-sessions) and preview state persist names and session ids with no cleanup; add a clearLocalData action that wipes per-session keys on leave or after a TTL. | On a shared machine the next user can read prior rooms' names, comments, and ids straight out of localStorage. |
| Curved segment names baked into SVG | M | 4 | exportWheelSVG already honors per-segment color_override and computeAngles weights but draws only a center title; add a textPath on each arc using the same cursor angles so the static SVG is self-labeling like the canvas. | A shared wheel image is useless if you cannot tell whose slice is whose. |
| Observed vs expected odds drift bars | M | 4 | For each name compute expected share from computeAngles weights and observed share from AnalyticsPanel's nameCounts over removed, then draw a diverging bar centered on expected so over- and under-luck read left and right. | Separates real weight bias from streaky luck, the core question of a fairness view. |
| CSV roster import with weight column | M | 4 | Extend TemplateGallery/useCreateSession to parse an uploaded CSV (name,weight) into add_participants_batch then per-row update_participant_props, respecting the server's 0.1-10 weight clamp. | Bulk-loads large rosters and per-name odds from a spreadsheet teams already keep. |
| Suggest undo when history non-empty | M | 4 | Pass history.canUndo and the last action's kind into useCommandPalette and emit a context command 'Undo: {action label}' calling history.undo(), shown first when the query is empty. | Undo is the most likely next intent right after a destructive command and Cmd-Z is not always at hand. |
| Scope spin capture to the actual spin | M | 4 | captureSpinGIF records a fixed wall-clock window unaware of playSpin's 3200ms duration; bound the MediaRecorder window to playSpin's launch and onComplete so the clip starts at launch and stops on the thunk. | The fixed window currently clips mid-spin or records dead frames after it lands. |
| Share winner card via native share | M | 4 | RecapReel already draws a trophy canvas and copies it via ClipboardItem; on touch devices route that canvas through navigator.share with a File so the card posts directly to chat. | Sharing the result is the social payoff of the app, and clipboard image paste is unreliable on phones. |
| Ghost names spin on empty wheel | M | 4 | While roster.activeParticipants is empty the renderer draws a dark placeholder ring; feed it dim placeholder segments built from NameList's SAMPLE_NAMES so the wheel renders a faint real ring behind the empty card. | A live demo wheel teaches the spin metaphor instantly and makes the first screen feel alive rather than broken. |
| Elimination order timeline strip | M | 4 | removedParticipants already carries spin_order and removed_at; lay them on a horizontal time axis from first-out to last-out with identityColor dots above AnalyticsPanel. | Sequence and pacing read richer than the current pick-count list and reuse data already present. |
| Bias curve presets dropdown | M | 4 | Add a preset selector in InspectorPanel that applies a weight distribution across activeParticipants (linear ramp, top-heavy, U-shaped) by index, calling updateWeight per participant and reusing effectiveForWheel for instant feedback. | Curating a whole distribution at once is far faster than nudging each name slider individually. |
| Chi-square fairness verdict chip | M | 4 | Replace AnalyticsPanel's mock biasEffect string (currently just weight !== 1) with a chi-square statistic comparing removed nameCounts against expected counts from preparedSegments weights, surfaced as a plain-language within-normal vs unusually-skewed chip. | Upgrades the placeholder 'No bias (uniform)' note into a statistically grounded read of the actual draw history. |
| Cumulative draw probability per name | M | 4 | rosterStore.simulateHeavy already runs N weighted draws in the worker; add an AnalyticsPanel column showing P(picked within next k spins) = 1 - product of survival odds, derived from preparedSegments weights. | Single-spin odds understate elimination risk in a multi-round game; cumulative odds answer what players actually ask. |
| Ritual preset save and recall | M | 4 | Persist the full rule set (preventRepeat, weights, mode flags, round count) as a named ritual in localStorage alongside the existing editor/panel keys; recall applies them via roster and uiStore setters in one logged action. | Facilitators run the same ceremony weekly; one-tap recall of 'Monday Standup rules' removes repetitive setup. |
| Cumulative odds-drift line chart | L | 4 | After each Spin in eventLog, recompute computeAngles for the shrinking roster and plot every remaining name's odds climbing over the run as stacked lines below AnalyticsPanel. | Visualizes the defining mechanic that staying on the wheel raises your own elimination odds. |
| Embeddable iframe snippet for the wheel | L | 4 | WheelCanvas.vue is a thin wrapper over the single three.js renderer; expose a /<id>/embed route that mounts only WheelCanvas plus a copyable <iframe> snippet next to copyLink in the dock. | Lets a wheel live inside a wiki, retro board, or stream overlay. |
| WebM export captures synthesized audio | L | 4 | useExports records WebM but audio is generated via WebAudio not the canvas; route masterGain into a MediaStreamAudioDestinationNode and mux its track into the export recorder. | Exported spin clips are silent today, and capturing the ticks and thunk makes shareable reels land. |
| Natural-language roster command bar | L | 4 | Add a palette command in useCommandPalette whose inline.submit routes free text ('add Alice Bob, drop Dave, weight Carol 3x') through an AI parser emitting rosterStore.addNames / removeName / updateWeight calls. | Turns the palette from fixed verbs into one sentence that mutates the whole roster, faster than per-name typing. |
| Survivor leaderboard across sessions | L | 4 | useRecents already tracks prior sessions; aggregate each session's survivor (the lone active participant at recap) into a cross-session 'most survived' board rendered like RecapReel rows. | Repeat groups want a running champion board, not just one night's recap. |
| Real animated GIF instead of WebM | L | 4 | captureSpinGIF in utils/export.ts is misnamed: it returns a video/webm Blob via MediaRecorder; route frames through a worker GIF encoder so the download becomes a true loopable spin.gif. | GIF pastes and autoplays in Slack, Discord, and issues where WebM does not. |
| Animated recap reel to WebM | L | 4 | RecapReel's reel rows already stagger in via the reel-sweep keyframe with --row-delay; record a canvas redraw of drawTrophy's rows revealing in that same order into a short WebM. | A few-second reveal of the night reads far better in a feed than a static card. |
| Guided three-step first run | L | 4 | Add a dismissible three-dot progress rail (name it, add 2+, spin) near the session header, advancing as roster.activeParticipants grows and the first spin fires, with a done flag in localStorage. | A lightweight checklist orients a first user across the create-populate-spin arc without a modal tour. |
| Ellipsis budget by pixel width | S | 3 | Swap makeSimpleLabel's name.length>18 char-count rule for ctx.measureText-based truncation that trims until the string fits the DPR-scaled canvas width, then appends the ellipsis. | Char-count truncation chops narrow names too early and wide names too late, while pixel measurement is correct for any glyph set. |
| Predict fairness before spinning | S | 3 | Read effectiveForWheel weights and run an AI plain-language forecast (Carol is 3x as likely as the rest) into a pre-spin hint, complementing NameList's odds bars. | Odds bars show proportion but not narrative, and a sentence makes a skewed wheel obvious to non-technical hosts. |
| Surface elevation shadow token ladder | S | 3 | Drop shadows are bespoke per surface (dock 0 6 20/.4, recents-menu 0 12 32/.5, panel none, tick glow 0 4 16/.35), so define --elev-1/2/3 box-shadow tokens mapped to z-layers. | Consistent elevation makes stacking order legible instead of randomly heavier shadows. |
| Tokenize tick-color glow into shared var | S | 3 | NameList sets --tick-color, ToastStack sets --toast-accent, and SpinResult sets --accent, each a private alias of the same identity hue, so collapse them into one inherited --entity-accent contract. | Three components reinvent the same per-name glow with slightly different var names and defaults. |
| Cumulative-odds stacked bar of field | S | 3 | Render a single horizontal stacked bar in AnalyticsPanel where each segment width is getOdds(p) and fill is identityColor(p.name), giving a 100%-stacked view of the whole distribution in one row. | One compact bar shows the entire probability split and how lopsided the field is at a glance. |
| Predicted next-out callout pre-spin | S | 3 | Before a spin take the argmax of computeAngles over activeParticipants and show a subtle 'most likely: name (NN%)' line in AnalyticsPanel. | Frames each spin with a stated favorite, raising tension cheaply from existing odds. |
| Privacy notice before remembering recents | S | 3 | rememberSession in useSession.ts silently writes {id,title,lastUsed} for every loaded room into localStorage under wheel-recent-sessions; show a first-run notice and an opt-out. | Storing room ids (the access secret) and titles on the device without disclosure is a quiet privacy leak on shared machines. |
| Consent prompt before voice capture | S | 3 | useVoice calls recognition.start() immediately on startVoiceAdd/voiceSpin, streaming audio to the browser's cloud recognizer; gate the first use behind a consent dialog that explains audio leaves the device and persist the choice. | SpeechRecognition streams microphone audio to a third party, a privacy event the user should knowingly opt into. |
| CSV export of the action log | S | 3 | Add exportActions() to useExports that GETs listActions and flattens ActionKind variants (Spin, Add, Remove, UpdateWeight) into CSV rows with timestamp and actor. | Complements the SVG, WebM, and PDF exports with a spreadsheet-friendly audit trail. |
| Round ceremony banner between spins | S | 3 | After onSpinComplete in App.vue, instead of immediately re-enabling Space, show a brief 'Round N closed, M remain' banner reusing SpinResult's pop-settle keyframe, dismissed by Space which also arms the next round. | A deliberate beat between rounds keeps a roomful of people synced before the next spin. |
| Tag weighted picks with their bias | S | 3 | When a Spin lands, check the picked id's engine weight; if above 1, tag SpinResult with 'weighted Nx' so a skewed result owns its odds instead of looking random. | Closes the trust gap when a heavily weighted name is picked and the room cries foul. |
| Confirm before destructive reset | S | 3 | reset_session in handlers.rs and resetRoster in roster.ts wipe removed state for all clients with one POST and a 'Session reset' toast; require an explicit host-scoped confirmation first. | Reset is a one-click irreversible change broadcast to everyone, ripe for accidental or malicious abuse. |
| Export elimination order and weights as CSV | S | 3 | exportPDF already reads removedParticipants (spin_order) and activeParticipants weights into a text blob; emit a proper results.csv with name, spin_order, weight, removed_at columns instead. | Spreadsheet users want the draw outcome as data, not prose. |
| Persist theme and accent to localStorage | S | 3 | ui.ts persists the editor flag and panel-collapsed state but currentTheme and themeColor reset on reload; add a theme key alongside EDITOR_KEY/PANEL_KEY so a chosen skin and accent survive refresh. | A theme that forgets itself every reload feels broken next to the persisted editor and panel state. |
| Theme and editor toggles as commands | S | 3 | Add palette commands wired to ui.toggleTheme and ui.toggleEditor with labels reflecting current state (like toggle-sound does), grouped under a View section. | The palette already owns toggle-sound, and theme and editor mode are the obvious missing siblings. |
| Bias budget meter for the host | S | 3 | AnalyticsPanel's biasEffect is a boolean string ('Bias active' vs 'No bias'); replace it with a magnitude that sums (weight-1) across weighted participants so the host sees how far the wheel is tilted from uniform. | Lets a host quantify and deliberately walk back how much they have tilted the wheel. |
| Per-round recap card, not just final | S | 3 | Emit a lightweight recap at each round complete by calling buildRecapSummary scoped to that round's removed slice, copyable from a small toast, rather than only the end-of-game RecapReel. | Multi-round ceremonies want a paste-able summary after each round for chat. |
| Emoji and grapheme-aware label truncation | S | 3 | makeSimpleLabel truncates with name.slice(0,17), splitting multi-codepoint graphemes; use Intl.Segmenter grapheme segmentation before truncating. | Names with emoji are common in casual rosters and char-index slicing produces broken replacement glyphs. |
| Editor mode unlock teaser | S | 3 | Inspector, History, and Analytics panels are gated behind isEditor (ui.editorEnabled) and invisible until found; when editor is off and the roster passes ~4 names, show a one-line dismissible hint near the panel-toggle row. | Editor-only power features never get tried because nothing points to the toggle. |
| Explain-this-weight inline note | S | 3 | InspectorPanel's bias row shows only the raw 0.1-10 slider; add a generated rationale that reads the participant's odds and explains the weight's effect, for example 3x means about 43 percent versus 14 percent for others. | The slider gives no sense of resulting odds, and a phrased consequence makes it legible. |
| Apple touch icon and theme color tags | S | 3 | index.html lacks apple-touch-icon, apple-mobile-web-app-status-bar-style, and theme-color, and the title is literally 'frontend'; add them with theme-color #1e1e1e to match the canvas clear color. | An installed iOS shortcut otherwise shows a blank icon and a white status bar against the dark canvas. |
| Wake lock during a session | S | 3 | A session is driven by Space-to-spin and WS broadcasts with the wheel as a fullscreen canvas; request navigator.wakeLock when a session loads and release it on unmount. | Phones dim and lock mid-game when the wheel sits idle between spins, breaking the shared-screen ritual. |
| Session summary stat header | S | 3 | Aggregate eventLog into a compact header above AnalyticsPanel (total spins, total weight edits, longest survivor streak, fairness verdict) replacing the terse Total/Removed/Avg one-liner. | Replaces a cramped one-liner with a real at-a-glance scoreboard. |
| Identity-colored SVG export segments | S | 3 | In exportWheelSVG each path falls back to a single '#4ECDC4' when no color_override exists (export.ts:19); swap that fallback to identityColor(p.name) so every segment carries its name-hash hue like the three.js build. | Exported segments read as the same people the audience just watched, not a wall of teal. |
| Weight-driven label boldness ramp | S | 3 | build already derives emissive from p.weight; reuse that weight to pick the canvas font weight (400 at w=1 up to 900 near the emissive cap) so heavier-odds names read visually heavier. | Ties the label's visual weight to the participant's literal weight, reinforcing odds at a glance. |
| Letter-spacing tightening on dense slices | S | 3 | For segments whose p.angle is below a threshold, apply negative ctx.letterSpacing in makeSimpleLabel to condense the name into the available chord before resorting to truncation. | Mild tracking buys several characters on thin slices, deferring the ugly ellipsis. |
| Replace fixed identity S/L with tokens | S | 3 | identity.ts pins hsl(hash, 62%, 66%); read --identity-sat and --identity-light from CSS (current values as defaults) so a skin can shift all segment colors toward pastel or neon while keeping the deterministic hue mapping. | Lets a theme restyle the entire roster palette globally without touching per-name hue stability. |
| Export action log as replay file | M | 3 | useExports handles SVG, spin capture, and a mock PDF but not history, so add a JSON export of api.listActions output to archive or re-import a session's full event log. | The event log is the canonical record of a game yet there is no way to save or share it as data. |
| Kerning via opentype glyph advances | M | 3 | makeSimpleLabel's canvas fillText ignores kerning, so render names through the already-loaded _otFont.getPath with proper kerning pairs into the label canvas. | The loaded opentype font carries kerning data the canvas path throws away, leaving loose AV/VA pairs. |
| Per-name spin-order distribution histogram | M | 3 | Run engine.simulate hundreds of times capturing each id's spin_order, then in the per-person drawer draw a small histogram of where that name typically goes out. | Shows the spread, not just the mean, of a person's expected fate. |
| Quick-add chips for common names | M | 3 | Under the empty NameList input, render recently-used name chips from a localStorage frequency store (mirroring useRecents) so repeat users re-seed a familiar roster in taps. | Teams reuse the same people, so one-tap chips beat retyping the same six names every session. |
| Recap narration from spin order | M | 3 | Feed removedParticipants (spin_order sorted) and the session title into an AI summarizer that returns a short prose paragraph, appended beside buildRecapSummary's plain list in RecapReel's copy actions. | Gives hosts a paste-ready story instead of only a numbered roster dump. |

### Delight (20)

| Title | Eff | Val | Mapping | Why |
|-------|-----|-----|---------|-----|
| Survivor coronation eulogy line | S | 4 | When activeParticipants drops to one, RecapReel/SpinResult shows a generated single-line eulogy stitched from removedParticipants spin_order and timestamps ('outlasted 7, dodged 4 spins over 12 minutes'). | Gives the last-standing moment a story instead of a bare 'survives' label. |
| Long-shot win unlocks a tale toast | S | 4 | When a sub-10-percent-odds name wins (computed from engine weights at spin start), push a sticky toast narrating it with the winner's identityColor accent. | It captures statistically rare moments as quotable lore the host can screenshot. |
| Names react to their own doom | M | 4 | As tickingId lands on a segment during the SLOWMO stretch, lift that segment's makeSimpleLabel slightly and add a tiny shiver wobble for the last few ticks, settling when the winner locks. | Personifies the wheel's victims in the most tense moment for a comedic micro-beat. |
| Doom clock for the final two | M | 4 | When activeParticipants.length===2 the header flame (useFlame) intensifies spawnPerFrame and tints redder and the dock Spin button gains a quiet 'Final Round' eyebrow, gated by prefers-reduced-motion. | Escalates tension at the climax using the flame that already breathes at the top of the app. |
| Eliminated names leave ghost epitaphs | M | 4 | On removal, before the segment collapses, briefly billboard the name in grayscale at its old rim angle (reusing makeSimpleLabel and spin_order) fading over about 1.2s like a tombstone. | Gives each elimination a small mournful beat instead of an instant disappearance. |
| Wheel remembers its grudges | M | 4 | Fold eventLog Spin actions in roster.ts into a per-name pick count; a name picked 3+ times gets a small 'cursed' ember glyph in NameList and a faint emissive flicker on its segment via updateInPlace. | Turns the action log into visible lore so repeat victims feel a running personal narrative. |
| Last-second reversal fanfare | M | 4 | In playSpin, compare the winner's pre-spin odds against the field; if it was the lowest-odds name, swap playThunk for a rising upset arpeggio and trigger a double-length flame flare. | It rewards the dramatic underdog elimination with audible and visual punctuation. |
| Confetti burst on winner reveal | L | 4 | Fire a GPU THREE.Points confetti system from onSpinComplete in ThreeWheelRenderer using identityColor(winner.name) plus complementary hues, spawned at the top pointer and falling with the wheel. | Winner reveal currently has only a CSS lower-third; 3D confetti in the wheel space ties the celebration to the object that produced it. |
| Confetti density scales with stakes | S | 3 | Scale the winner confetti burst by remaining count from onWinnerReveal data so fewer remaining means a bigger finale, peaking on the last elimination that triggers recapQueued. | Ties VFX intensity to game state so the final shame reveal earns a bigger celebration than an early one. |
| Underdog and favorite callouts | S | 3 | From oddsById pick argmin and argmax getOdds and stamp small longest-odds and most-exposed chips on those two NameList rows, recomputed on refreshPreparedSegments. | Surfaces the narrative extremes of the distribution without the host scanning every percentage. |
| Per-session accent from title hash | S | 3 | identityColor already hashes names to hues and session.title is available in App.vue; optionally derive a session-default --theme-accent from hashName(session.title) so each room has a distinct tint. | Gives every session a recognizable color signature with zero new persistence, reusing the hash util. |
| Recap reel narrator voice | S | 3 | Add an optional button in RecapReel that reads buildRecapSummary aloud via SpeechSynthesis in a game-show-host cadence, reusing the recap text already assembled. | Turns the share summary into a performable closing moment for the room. |
| First-spin reveal is earned | S | 3 | Gate a richer reveal (extra flare duration in onWinnerReveal) on the first-ever spin tracked by a localStorage flag, so the inaugural elimination feels like a milestone. | Amplifying the very first result rewards the user for completing onboarding. |
| Eliminated bucket as a graveyard | S | 3 | Restyle the removedParticipants section header in NameList to read 'The Fallen' with each row's removed_at rendered as a relative 'fell 3m ago' epitaph timestamp. | Reframes a plain removed-list into thematic lore consistent with Wheel of Shame. |
| Wheel of Fortune host intro | S | 3 | First spin of a fresh session plays a short count-in flourish (4 accent ticks via playTick) and a one-line 'And now... the wheel decides' toast, gated once per session id. | A ceremonial opening beat frames the session as a show without slowing repeat spins. |
| Live accent drives palette and toast | S | 3 | --live-accent already animates on .overlay during spins via @property but only the dock hairline and vignette sample it; let ToastStack's spin-result toast and SpinResult's lower-third inherit it so the reveal shares the winner hue. | Reuses an existing animated token to make the winner reveal feel color-coordinated end to end. |
| Spin counter as wheel mileage | S | 3 | Count Spin actions in eventLog and render a faint odometer-style 'Spin #14' watermark near the wheel hub via a billboarded label, ticking up on each playSpin completion. | Frames the session as an accruing journey and makes heavy-use sessions feel earned. |
| Wheel whispers idle taunts | S | 3 | After ~20s idle with names present, occasionally surface a low-key info toast with a deterministic taunt seeded by activeParticipants names, capped to one per idle stretch. | Personifies the wheel during dead air without nagging, leaning on the existing toast queue. |
| Palette command for a gentle roast | S | 3 | Add a hidden palette command that pushes a deterministic jab toast about the current highest-odds name, seeded by their name hash so it is stable. | A discoverable bit of personality fits the palette's existing action grammar. |
| Seasonal flame palette | S | 3 | Branch the color ramp in useFlame's particle loop by current month (orange/black late October, blue-white December, default otherwise), all behind the existing reducedMotion guard. | Quiet seasonal warmth on the signature flame header that users rediscover over the year. |

---

## Method

- Generated 960 raw ideas across 28 domains; code-deduped to 960; curated to 749 keepers; ranked top 500.
- Domains: spin-physics, audio-design, haptics-physicality, camera-cinematography, particle-vfx, typography-labels, odds-dataviz, weight-editing, history-timetravel, keyboard-grammar, command-palette, pointer-gestures, accessibility, mobile-touch, collaboration-presence, facilitation-rituals, onboarding-emptystates, theming-design-system, export-share, analytics-insights, backend-datamodel, realtime-networking, security-privacy, performance-rendering, testing-devex, ai-intelligence, integrations, delight-narrative.
- Dedup was title-normalized + an existing-concept guard; conceptual near-duplicates with different wording may remain.

